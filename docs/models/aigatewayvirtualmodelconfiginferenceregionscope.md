# AiGatewayVirtualModelConfigInferenceRegionScope

Pin scope: `specific` (one provider region), `zone` (geo zone), or `global`.

## Example Usage

```typescript
import { AiGatewayVirtualModelConfigInferenceRegionScope } from "@vercel/sdk/models/aigatewayvirtualmodelconfig.js";

let value: AiGatewayVirtualModelConfigInferenceRegionScope = "zone";
```

## Values

```typescript
"global" | "specific" | "zone"
```