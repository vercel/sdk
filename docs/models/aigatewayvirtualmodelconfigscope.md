# AiGatewayVirtualModelConfigScope

Pin scope: `specific` (one provider region), `zone` (geo zone), or `global`.

## Example Usage

```typescript
import { AiGatewayVirtualModelConfigScope } from "@vercel/sdk/models/aigatewayvirtualmodelconfig.js";

let value: AiGatewayVirtualModelConfigScope = "zone";
```

## Values

```typescript
"global" | "specific" | "zone"
```