# CompleteRollingReleaseSubstate

When set to `PAUSED`, the rollout is frozen at the current percentage until continued.

## Example Usage

```typescript
import { CompleteRollingReleaseSubstate } from "@vercel/sdk/models/completerollingreleaseop.js";

let value: CompleteRollingReleaseSubstate = "PAUSED";
```

## Values

```typescript
"PAUSED"
```