# CreateDeploymentDestinationDeploymentsResponseType

Optional explicit format marker. The destination is identified by the presence of `service`, so `type` is no longer required.

## Example Usage

```typescript
import { CreateDeploymentDestinationDeploymentsResponseType } from "@vercel/sdk/models/createdeploymentresponsebody.js";

let value: CreateDeploymentDestinationDeploymentsResponseType = "service";
```

## Values

```typescript
"service"
```