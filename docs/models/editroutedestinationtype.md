# EditRouteDestinationType

Optional explicit format marker. The destination is identified by the presence of `service`, so `type` is no longer required.

## Example Usage

```typescript
import { EditRouteDestinationType } from "@vercel/sdk/models/editrouteop.js";

let value: EditRouteDestinationType = "service";
```

## Values

```typescript
"service"
```