# GetRoutesDestinationProjectRoutesResponseType

Optional explicit format marker. The destination is identified by the presence of `service`, so `type` is no longer required.

## Example Usage

```typescript
import { GetRoutesDestinationProjectRoutesResponseType } from "@vercel/sdk/models/getroutesop.js";

let value: GetRoutesDestinationProjectRoutesResponseType = "service";
```

## Values

```typescript
"service"
```