# GetRoutesResponseBodyProjectRoutesRouteType

Computed route type based on the route configuration. Only present in API responses, not stored in S3.

## Example Usage

```typescript
import { GetRoutesResponseBodyProjectRoutesRouteType } from "@vercel/sdk/models/getroutesop.js";

let value: GetRoutesResponseBodyProjectRoutesRouteType = "set_status";
```

## Values

```typescript
"redirect" | "rewrite" | "set_status" | "transform"
```