# PayloadProvider

Present on new events only. Equivalent to "stripe" when absent.

## Example Usage

```typescript
import { PayloadProvider } from "@vercel/sdk/models/payload1.js";

let value: PayloadProvider = "stripe";
```

## Values

```typescript
"chatgpt" | "stripe"
```