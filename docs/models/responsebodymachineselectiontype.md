# ResponseBodyMachineSelectionType

Whether the build ran on a fixed or elastic machine. Used to drive billing for the build.

## Example Usage

```typescript
import { ResponseBodyMachineSelectionType } from "@vercel/sdk/models/createdeploymentrouteshas.js";

let value: ResponseBodyMachineSelectionType = "fixed";
```

## Values

```typescript
"elastic" | "fixed"
```