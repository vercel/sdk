# Substate

When set to `PAUSED`, the rollout is frozen at the current percentage until continued.

## Example Usage

```typescript
import { Substate } from "@vercel/sdk/models/getrollingreleaseop.js";

let value: Substate = "PAUSED";
```

## Values

```typescript
"PAUSED"
```