/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assert, expect, it, test } from "vitest";
import { Vercel } from "../index.js";
import { createTestHTTPClient } from "./testclient.js";

it.skip("Connect Update Shared Connect Links", async () => {
  assert.fail(
    "incomplete test found please make sure to address the following errors: [`workflow step updateSharedConnectLinks.test referencing operation updateSharedConnectLinks not found in document`]",
  );
});

test("Connect Create Connector", async () => {
  const testHttpClient = createTestHTTPClient("createConnector");

  const vercel = new Vercel({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearerToken: "<YOUR_BEARER_TOKEN_HERE>",
  });

  const result = await vercel.connect.createConnector();
  expect(result).toBeDefined();
});

test("Connect Get Connector Token", async () => {
  const testHttpClient = createTestHTTPClient("getConnectorToken");

  const vercel = new Vercel({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearerToken: "<YOUR_BEARER_TOKEN_HERE>",
  });

  const result = await vercel.connect.getConnectorToken({
    connector: "<value>",
  });
  expect(result).toBeDefined();
});

test("Connect Create Connector Authorization Request", async () => {
  const testHttpClient = createTestHTTPClient(
    "createConnectorAuthorizationRequest",
  );

  const vercel = new Vercel({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearerToken: "<YOUR_BEARER_TOKEN_HERE>",
  });

  const result = await vercel.connect.createConnectorAuthorizationRequest({
    connector: "<value>",
  });
  expect(result).toBeDefined();
});
