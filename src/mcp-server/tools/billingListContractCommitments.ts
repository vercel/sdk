/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { billingListContractCommitments } from "../../funcs/billingListContractCommitments.js";
import { ListContractCommitmentsRequest$inboundSchema } from "../../models/listcontractcommitmentsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListContractCommitmentsRequest$inboundSchema,
};

export const tool$billingListContractCommitments: ToolDefinition<typeof args> =
  {
    name: "billing-list-contract-commitments",
    description: `List FOCUS contract commitments

Returns commitment allocations per contract period in FOCUS v1.3 JSONL format for a specified Vercel team. The response is streamed as newline-delimited JSON (JSONL). This endpoint is only applicable to Enterprise Vercel customers. An empty response is returned for non-Enterprise (Pro/Flex) customers.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await billingListContractCommitments(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
