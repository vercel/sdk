/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checksV2CreateDeploymentCheckRun } from "../../funcs/checksV2CreateDeploymentCheckRun.js";
import { CreateDeploymentCheckRunRequest$inboundSchema } from "../../models/createdeploymentcheckrunop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateDeploymentCheckRunRequest$inboundSchema,
};

export const tool$checksV2CreateDeploymentCheckRun: ToolDefinition<
  typeof args
> = {
  name: "checks-v2-create-deployment-check-run",
  description: `Create a check run

Creates a new check run for a deployment.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checksV2CreateDeploymentCheckRun(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
