/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checksV2CreateProjectCheck } from "../../funcs/checksV2CreateProjectCheck.js";
import { CreateProjectCheckRequest$inboundSchema } from "../../models/createprojectcheckop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateProjectCheckRequest$inboundSchema,
};

export const tool$checksV2CreateProjectCheck: ToolDefinition<typeof args> = {
  name: "checks-v2-create-project-check",
  description: `Create a check

Creates a new check for a project.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checksV2CreateProjectCheck(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
