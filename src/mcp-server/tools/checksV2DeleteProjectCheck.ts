/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checksV2DeleteProjectCheck } from "../../funcs/checksV2DeleteProjectCheck.js";
import { DeleteProjectCheckRequest$inboundSchema } from "../../models/deleteprojectcheckop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteProjectCheckRequest$inboundSchema,
};

export const tool$checksV2DeleteProjectCheck: ToolDefinition<typeof args> = {
  name: "checks-v2-delete-project-check",
  description: `Delete a check

Delete an existing check and all of its runs.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checksV2DeleteProjectCheck(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
