/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checksV2GetDeploymentCheckRun } from "../../funcs/checksV2GetDeploymentCheckRun.js";
import { GetDeploymentCheckRunRequest$inboundSchema } from "../../models/getdeploymentcheckrunop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetDeploymentCheckRunRequest$inboundSchema,
};

export const tool$checksV2GetDeploymentCheckRun: ToolDefinition<typeof args> = {
  name: "checks-v2-get-deployment-check-run",
  description: `Get a check run

Return a detailed response for a single check run.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checksV2GetDeploymentCheckRun(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
