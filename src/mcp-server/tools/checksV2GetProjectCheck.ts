/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checksV2GetProjectCheck } from "../../funcs/checksV2GetProjectCheck.js";
import { GetProjectCheckRequest$inboundSchema } from "../../models/getprojectcheckop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetProjectCheckRequest$inboundSchema,
};

export const tool$checksV2GetProjectCheck: ToolDefinition<typeof args> = {
  name: "checks-v2-get-project-check",
  description: `Get a check

Return a detailed response for a single check.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checksV2GetProjectCheck(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
