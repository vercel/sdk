/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checksV2ListCheckRuns } from "../../funcs/checksV2ListCheckRuns.js";
import { ListCheckRunsRequest$inboundSchema } from "../../models/listcheckrunsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListCheckRunsRequest$inboundSchema,
};

export const tool$checksV2ListCheckRuns: ToolDefinition<typeof args> = {
  name: "checks-v2-list-check-runs",
  description: `List runs for a check

List all runs associated with a given check.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checksV2ListCheckRuns(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
