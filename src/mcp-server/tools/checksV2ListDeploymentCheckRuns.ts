/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checksV2ListDeploymentCheckRuns } from "../../funcs/checksV2ListDeploymentCheckRuns.js";
import { ListDeploymentCheckRunsRequest$inboundSchema } from "../../models/listdeploymentcheckrunsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListDeploymentCheckRunsRequest$inboundSchema,
};

export const tool$checksV2ListDeploymentCheckRuns: ToolDefinition<typeof args> =
  {
    name: "checks-v2-list-deployment-check-runs",
    description: `List check runs for a deployment

List all check runs for a deployment.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await checksV2ListDeploymentCheckRuns(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
