/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checksV2ListProjectChecks } from "../../funcs/checksV2ListProjectChecks.js";
import { ListProjectChecksRequest$inboundSchema } from "../../models/listprojectchecksop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListProjectChecksRequest$inboundSchema,
};

export const tool$checksV2ListProjectChecks: ToolDefinition<typeof args> = {
  name: "checks-v2-list-project-checks",
  description: `List all checks for a project

List all checks for a project, optionally filtered by target.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checksV2ListProjectChecks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
