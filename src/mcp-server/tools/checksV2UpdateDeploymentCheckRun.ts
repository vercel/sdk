/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checksV2UpdateDeploymentCheckRun } from "../../funcs/checksV2UpdateDeploymentCheckRun.js";
import { UpdateDeploymentCheckRunRequest$inboundSchema } from "../../models/updatedeploymentcheckrunop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateDeploymentCheckRunRequest$inboundSchema,
};

export const tool$checksV2UpdateDeploymentCheckRun: ToolDefinition<
  typeof args
> = {
  name: "checks-v2-update-deployment-check-run",
  description: `Update a check run

Update an existing check run for a deployment.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checksV2UpdateDeploymentCheckRun(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
