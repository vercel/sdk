/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checksV2UpdateProjectCheck } from "../../funcs/checksV2UpdateProjectCheck.js";
import { UpdateProjectCheckRequest$inboundSchema } from "../../models/updateprojectcheckop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateProjectCheckRequest$inboundSchema,
};

export const tool$checksV2UpdateProjectCheck: ToolDefinition<typeof args> = {
  name: "checks-v2-update-project-check",
  description: `Update a check

Update an existing check.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checksV2UpdateProjectCheck(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
