/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectCreateConnector } from "../../funcs/connectCreateConnector.js";
import { CreateConnectorRequestBody$inboundSchema } from "../../models/createconnectorop.js";
import * as types from "../../types/primitives.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: types.optional(CreateConnectorRequestBody$inboundSchema),
};

export const tool$connectCreateConnector: ToolDefinition<typeof args> = {
  name: "connect-create-connector",
  description: `Create a connector

Create a connector from type-specific configuration and optionally link it to a project during creation.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await connectCreateConnector(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
