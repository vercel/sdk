/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectCreateConnectorAuthorizationRequest } from "../../funcs/connectCreateConnectorAuthorizationRequest.js";
import { CreateConnectorAuthorizationRequestRequest$inboundSchema } from "../../models/createconnectorauthorizationrequestop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateConnectorAuthorizationRequestRequest$inboundSchema,
};

export const tool$connectCreateConnectorAuthorizationRequest: ToolDefinition<
  typeof args
> = {
  name: "connect-create-connector-authorization-request",
  description: `Create a Connect authorization request

Create an authorization request for a connector and return the URL and verifier details needed to complete the flow.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await connectCreateConnectorAuthorizationRequest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
