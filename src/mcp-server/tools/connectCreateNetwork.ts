/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectCreateNetwork } from "../../funcs/connectCreateNetwork.js";
import { CreateNetworkRequest$inboundSchema } from "../../models/createnetworkop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateNetworkRequest$inboundSchema,
};

export const tool$connectCreateNetwork: ToolDefinition<typeof args> = {
  name: "connect-create-network",
  description: `Create a Secure Compute network

Allows to create a Secure Compute network.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await connectCreateNetwork(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
