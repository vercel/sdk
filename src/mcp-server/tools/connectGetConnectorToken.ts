/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectGetConnectorToken } from "../../funcs/connectGetConnectorToken.js";
import { GetConnectorTokenRequest$inboundSchema } from "../../models/getconnectortokenop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetConnectorTokenRequest$inboundSchema,
};

export const tool$connectGetConnectorToken: ToolDefinition<typeof args> = {
  name: "connect-get-connector-token",
  description: `Get a Connect token

Get an access token for a connector identified by the path parameter and scoped to the requester.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await connectGetConnectorToken(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
