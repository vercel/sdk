/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectListNetworks } from "../../funcs/connectListNetworks.js";
import { ListNetworksRequest$inboundSchema } from "../../models/listnetworksop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListNetworksRequest$inboundSchema,
};

export const tool$connectListNetworks: ToolDefinition<typeof args> = {
  name: "connect-list-networks",
  description: `List Secure Compute networks

Allows to list Secure Compute networks.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await connectListNetworks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
