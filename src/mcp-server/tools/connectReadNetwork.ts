/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectReadNetwork } from "../../funcs/connectReadNetwork.js";
import { ReadNetworkRequest$inboundSchema } from "../../models/readnetworkop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ReadNetworkRequest$inboundSchema,
};

export const tool$connectReadNetwork: ToolDefinition<typeof args> = {
  name: "connect-read-network",
  description: `Read a Secure Compute network

Allows to read a Secure Compute network.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await connectReadNetwork(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
