/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectUpdateNetwork } from "../../funcs/connectUpdateNetwork.js";
import { UpdateNetworkRequest$inboundSchema } from "../../models/updatenetworkop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateNetworkRequest$inboundSchema,
};

export const tool$connectUpdateNetwork: ToolDefinition<typeof args> = {
  name: "connect-update-network",
  description: `Update a Secure Compute network

Allows to update a Secure Compute network.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await connectUpdateNetwork(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
