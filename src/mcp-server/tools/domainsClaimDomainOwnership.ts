/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { domainsClaimDomainOwnership } from "../../funcs/domainsClaimDomainOwnership.js";
import { ClaimDomainOwnershipRequest$inboundSchema } from "../../models/claimdomainownershipop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ClaimDomainOwnershipRequest$inboundSchema,
};

export const tool$domainsClaimDomainOwnership: ToolDefinition<typeof args> = {
  name: "domains-claim-domain-ownership",
  description: `Claim Domain Ownership

Claim ownership of a domain for the authenticated team by verifying a TXT record. The caller must first add a TXT record to \`_vercel.{domain}\` (obtained from GET /domains/:domain/verification), then call this endpoint to complete the ownership transfer. If the TXT record is verified, the domain ownership will be transferred to the caller's team, even if the domain is currently owned by another user or team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await domainsClaimDomainOwnership(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
