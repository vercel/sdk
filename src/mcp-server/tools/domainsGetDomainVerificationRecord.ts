/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { domainsGetDomainVerificationRecord } from "../../funcs/domainsGetDomainVerificationRecord.js";
import { GetDomainVerificationRecordRequest$inboundSchema } from "../../models/getdomainverificationrecordop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetDomainVerificationRecordRequest$inboundSchema,
};

export const tool$domainsGetDomainVerificationRecord: ToolDefinition<
  typeof args
> = {
  name: "domains-get-domain-verification-record",
  description: `Get Domain Verification Record

Get the TXT verification record needed to claim ownership of a domain for the authenticated team. The caller must add this TXT record to \`_vercel.{domain}\` in their DNS configuration, then call POST /domains/:domain/claim to complete the ownership transfer.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await domainsGetDomainVerificationRecord(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
