/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { domainsRegistrarGetDomainAuthCode } from "../../funcs/domainsRegistrarGetDomainAuthCode.js";
import { GetDomainAuthCodeRequest$inboundSchema } from "../../models/getdomainauthcodeop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetDomainAuthCodeRequest$inboundSchema,
};

export const tool$domainsRegistrarGetDomainAuthCode: ToolDefinition<
  typeof args
> = {
  name: "domains-registrar-get-domain-auth-code",
  description: `Get the auth code for a domain

Get the auth code for a domain. This is required to transfer a domain from Vercel to another registrar.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await domainsRegistrarGetDomainAuthCode(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
