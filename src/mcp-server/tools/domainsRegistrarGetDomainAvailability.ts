/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { domainsRegistrarGetDomainAvailability } from "../../funcs/domainsRegistrarGetDomainAvailability.js";
import { GetDomainAvailabilityRequest$inboundSchema } from "../../models/getdomainavailabilityop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetDomainAvailabilityRequest$inboundSchema,
};

export const tool$domainsRegistrarGetDomainAvailability: ToolDefinition<
  typeof args
> = {
  name: "domains-registrar-get-domain-availability",
  description: `Get availability for a specific domain

Get availability for a specific domain`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await domainsRegistrarGetDomainAvailability(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
