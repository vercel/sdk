/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { domainsRegistrarGetDomainPrice } from "../../funcs/domainsRegistrarGetDomainPrice.js";
import { GetDomainPriceRequest$inboundSchema } from "../../models/getdomainpriceop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetDomainPriceRequest$inboundSchema,
};

export const tool$domainsRegistrarGetDomainPrice: ToolDefinition<typeof args> =
  {
    name: "domains-registrar-get-domain-price",
    description: `Get price data for a domain

Get price data for a specific domain`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await domainsRegistrarGetDomainPrice(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
