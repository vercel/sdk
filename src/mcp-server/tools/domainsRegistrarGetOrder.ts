/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { domainsRegistrarGetOrder } from "../../funcs/domainsRegistrarGetOrder.js";
import { GetOrderRequest$inboundSchema } from "../../models/getorderop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetOrderRequest$inboundSchema,
};

export const tool$domainsRegistrarGetOrder: ToolDefinition<typeof args> = {
  name: "domains-registrar-get-order",
  description: `Get a domain order

Get information about a domain order by its ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await domainsRegistrarGetOrder(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
