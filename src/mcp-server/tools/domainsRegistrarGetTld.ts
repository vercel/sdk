/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { domainsRegistrarGetTld } from "../../funcs/domainsRegistrarGetTld.js";
import { GetTldRequest$inboundSchema } from "../../models/gettldop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetTldRequest$inboundSchema,
};

export const tool$domainsRegistrarGetTld: ToolDefinition<typeof args> = {
  name: "domains-registrar-get-tld",
  description: `Get TLD

Get the metadata for a specific TLD.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await domainsRegistrarGetTld(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
