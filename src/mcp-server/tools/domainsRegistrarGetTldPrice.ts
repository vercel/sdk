/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { domainsRegistrarGetTldPrice } from "../../funcs/domainsRegistrarGetTldPrice.js";
import { GetTldPriceRequest$inboundSchema } from "../../models/gettldpriceop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetTldPriceRequest$inboundSchema,
};

export const tool$domainsRegistrarGetTldPrice: ToolDefinition<typeof args> = {
  name: "domains-registrar-get-tld-price",
  description: `Get price data for a specific TLD

Get TLD price data`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await domainsRegistrarGetTldPrice(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
