/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { domainsRegistrarRenewDomain } from "../../funcs/domainsRegistrarRenewDomain.js";
import { RenewDomainRequest$inboundSchema } from "../../models/renewdomainop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RenewDomainRequest$inboundSchema,
};

export const tool$domainsRegistrarRenewDomain: ToolDefinition<typeof args> = {
  name: "domains-registrar-renew-domain",
  description: `Renew a domain

Renew a domain`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await domainsRegistrarRenewDomain(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
