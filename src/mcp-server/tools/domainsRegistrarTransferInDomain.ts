/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { domainsRegistrarTransferInDomain } from "../../funcs/domainsRegistrarTransferInDomain.js";
import { TransferInDomainRequest$inboundSchema } from "../../models/transferindomainop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TransferInDomainRequest$inboundSchema,
};

export const tool$domainsRegistrarTransferInDomain: ToolDefinition<
  typeof args
> = {
  name: "domains-registrar-transfer-in-domain",
  description: `Transfer-in a domain

Transfer a domain in from another registrar`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await domainsRegistrarTransferInDomain(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
