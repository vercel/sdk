/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { domainsRegistrarUpdateDomainAutoRenew } from "../../funcs/domainsRegistrarUpdateDomainAutoRenew.js";
import { UpdateDomainAutoRenewRequest$inboundSchema } from "../../models/updatedomainautorenewop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateDomainAutoRenewRequest$inboundSchema,
};

export const tool$domainsRegistrarUpdateDomainAutoRenew: ToolDefinition<
  typeof args
> = {
  name: "domains-registrar-update-domain-auto-renew",
  description: `Update auto-renew for a domain

Update the auto-renew setting for a domain`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await domainsRegistrarUpdateDomainAutoRenew(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
