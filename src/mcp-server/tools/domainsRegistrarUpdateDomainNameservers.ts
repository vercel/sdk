/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { domainsRegistrarUpdateDomainNameservers } from "../../funcs/domainsRegistrarUpdateDomainNameservers.js";
import { UpdateDomainNameserversRequest$inboundSchema } from "../../models/updatedomainnameserversop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateDomainNameserversRequest$inboundSchema,
};

export const tool$domainsRegistrarUpdateDomainNameservers: ToolDefinition<
  typeof args
> = {
  name: "domains-registrar-update-domain-nameservers",
  description: `Update nameservers for a domain

Update the nameservers for a domain. Pass an empty array to use Vercel's default nameservers.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await domainsRegistrarUpdateDomainNameservers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
