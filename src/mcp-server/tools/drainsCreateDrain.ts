/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { drainsCreateDrain } from "../../funcs/drainsCreateDrain.js";
import { CreateDrainRequest$inboundSchema } from "../../models/createdrainop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateDrainRequest$inboundSchema,
};

export const tool$drainsCreateDrain: ToolDefinition<typeof args> = {
  name: "drains-create-drain",
  description: `Create a new Drain

Create a new Drain with the provided configuration.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await drainsCreateDrain(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
