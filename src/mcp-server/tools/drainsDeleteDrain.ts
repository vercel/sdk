/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { drainsDeleteDrain } from "../../funcs/drainsDeleteDrain.js";
import { DeleteDrainRequest$inboundSchema } from "../../models/deletedrainop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteDrainRequest$inboundSchema,
};

export const tool$drainsDeleteDrain: ToolDefinition<typeof args> = {
  name: "drains-delete-drain",
  description: `Delete a drain

Delete a specific Drain by passing the drain id in the URL.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await drainsDeleteDrain(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
