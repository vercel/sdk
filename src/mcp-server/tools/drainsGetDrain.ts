/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { drainsGetDrain } from "../../funcs/drainsGetDrain.js";
import { GetDrainRequest$inboundSchema } from "../../models/getdrainop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetDrainRequest$inboundSchema,
};

export const tool$drainsGetDrain: ToolDefinition<typeof args> = {
  name: "drains-get-drain",
  description: `Find a Drain by id

Get the information for a specific Drain by passing the drain id in the URL.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await drainsGetDrain(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
