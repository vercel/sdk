/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { drainsGetDrains } from "../../funcs/drainsGetDrains.js";
import { GetDrainsRequest$inboundSchema } from "../../models/getdrainsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetDrainsRequest$inboundSchema,
};

export const tool$drainsGetDrains: ToolDefinition<typeof args> = {
  name: "drains-get-drains",
  description: `Retrieve a list of all Drains

Allows to retrieve the list of Drains of the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await drainsGetDrains(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
