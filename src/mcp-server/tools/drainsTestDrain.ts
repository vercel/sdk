/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { drainsTestDrain } from "../../funcs/drainsTestDrain.js";
import { TestDrainRequest$inboundSchema } from "../../models/testdrainop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TestDrainRequest$inboundSchema,
};

export const tool$drainsTestDrain: ToolDefinition<typeof args> = {
  name: "drains-test-drain",
  description: `Validate Drain delivery configuration

Validate the delivery configuration of a Drain using sample events.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await drainsTestDrain(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
