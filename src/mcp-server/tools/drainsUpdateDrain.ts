/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { drainsUpdateDrain } from "../../funcs/drainsUpdateDrain.js";
import { UpdateDrainRequest$inboundSchema } from "../../models/updatedrainop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateDrainRequest$inboundSchema,
};

export const tool$drainsUpdateDrain: ToolDefinition<typeof args> = {
  name: "drains-update-drain",
  description: `Update an existing Drain

Update the configuration of an existing drain.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await drainsUpdateDrain(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
