/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { environmentCreateSharedEnvVariable } from "../../funcs/environmentCreateSharedEnvVariable.js";
import { CreateSharedEnvVariableRequest$inboundSchema } from "../../models/createsharedenvvariableop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateSharedEnvVariableRequest$inboundSchema,
};

export const tool$environmentCreateSharedEnvVariable: ToolDefinition<
  typeof args
> = {
  name: "environment-create-shared-env-variable",
  description: `Create one or more shared environment variables

Creates shared environment variable(s) for a team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await environmentCreateSharedEnvVariable(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
