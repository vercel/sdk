/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { environmentDeleteSharedEnvVariable } from "../../funcs/environmentDeleteSharedEnvVariable.js";
import { DeleteSharedEnvVariableRequest$inboundSchema } from "../../models/deletesharedenvvariableop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteSharedEnvVariableRequest$inboundSchema,
};

export const tool$environmentDeleteSharedEnvVariable: ToolDefinition<
  typeof args
> = {
  name: "environment-delete-shared-env-variable",
  description: `Delete one or more Env Var

Deletes one or many Shared Environment Variables for a given team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await environmentDeleteSharedEnvVariable(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
