/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { environmentListSharedEnvVariable } from "../../funcs/environmentListSharedEnvVariable.js";
import { ListSharedEnvVariableRequest$inboundSchema } from "../../models/listsharedenvvariableop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListSharedEnvVariableRequest$inboundSchema,
};

export const tool$environmentListSharedEnvVariable: ToolDefinition<
  typeof args
> = {
  name: "environment-list-shared-env-variable",
  description: `Lists all Shared Environment Variables for a team

Lists all Shared Environment Variables for a team, taking into account optional filters.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await environmentListSharedEnvVariable(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
