/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { environmentUnlinkSharedEnvVariable } from "../../funcs/environmentUnlinkSharedEnvVariable.js";
import { UnlinkSharedEnvVariableRequest$inboundSchema } from "../../models/unlinksharedenvvariableop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UnlinkSharedEnvVariableRequest$inboundSchema,
};

export const tool$environmentUnlinkSharedEnvVariable: ToolDefinition<
  typeof args
> = {
  name: "environment-unlink-shared-env-variable",
  description: `Disconnects a shared environment variable for a given project

Disconnects a shared environment variable for a given project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await environmentUnlinkSharedEnvVariable(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
