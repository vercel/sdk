/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { environmentUpdateSharedEnvVariable } from "../../funcs/environmentUpdateSharedEnvVariable.js";
import { UpdateSharedEnvVariableRequest$inboundSchema } from "../../models/updatesharedenvvariableop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateSharedEnvVariableRequest$inboundSchema,
};

export const tool$environmentUpdateSharedEnvVariable: ToolDefinition<
  typeof args
> = {
  name: "environment-update-shared-env-variable",
  description: `Updates one or more shared environment variables

Updates a given Shared Environment Variable for a Team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await environmentUpdateSharedEnvVariable(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
