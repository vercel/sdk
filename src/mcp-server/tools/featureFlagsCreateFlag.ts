/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { featureFlagsCreateFlag } from "../../funcs/featureFlagsCreateFlag.js";
import { CreateFlagRequest$inboundSchema } from "../../models/createflagop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateFlagRequest$inboundSchema,
};

export const tool$featureFlagsCreateFlag: ToolDefinition<typeof args> = {
  name: "feature-flags-create-flag",
  description: `Create a flag

Create a new feature flag for a project. The flag must have a unique slug within the project and specify its kind (boolean, string, or number).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await featureFlagsCreateFlag(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
