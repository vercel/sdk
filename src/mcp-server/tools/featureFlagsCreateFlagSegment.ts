/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { featureFlagsCreateFlagSegment } from "../../funcs/featureFlagsCreateFlagSegment.js";
import { CreateFlagSegmentRequest$inboundSchema } from "../../models/createflagsegmentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateFlagSegmentRequest$inboundSchema,
};

export const tool$featureFlagsCreateFlagSegment: ToolDefinition<typeof args> = {
  name: "feature-flags-create-flag-segment",
  description: `Create a segment

Create a new feature flag segment.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await featureFlagsCreateFlagSegment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
