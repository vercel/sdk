/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { featureFlagsCreateSDKKey } from "../../funcs/featureFlagsCreateSDKKey.js";
import { CreateSDKKeyRequest$inboundSchema } from "../../models/createsdkkeyop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateSDKKeyRequest$inboundSchema,
};

export const tool$featureFlagsCreateSDKKey: ToolDefinition<typeof args> = {
  name: "feature-flags-create-SDK-key",
  description: `Create an SDK key

Creates an SDK key.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await featureFlagsCreateSDKKey(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
