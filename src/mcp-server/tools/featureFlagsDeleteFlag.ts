/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { featureFlagsDeleteFlag } from "../../funcs/featureFlagsDeleteFlag.js";
import { DeleteFlagRequest$inboundSchema } from "../../models/deleteflagop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteFlagRequest$inboundSchema,
};

export const tool$featureFlagsDeleteFlag: ToolDefinition<typeof args> = {
  name: "feature-flags-delete-flag",
  description: `Delete a flag

Permanently delete a feature flag from the project. This action cannot be undone. Consider archiving the flag instead if you may need it in the future.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await featureFlagsDeleteFlag(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
