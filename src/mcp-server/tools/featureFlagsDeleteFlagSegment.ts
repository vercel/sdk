/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { featureFlagsDeleteFlagSegment } from "../../funcs/featureFlagsDeleteFlagSegment.js";
import { DeleteFlagSegmentRequest$inboundSchema } from "../../models/deleteflagsegmentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteFlagSegmentRequest$inboundSchema,
};

export const tool$featureFlagsDeleteFlagSegment: ToolDefinition<typeof args> = {
  name: "feature-flags-delete-flag-segment",
  description: `Delete a segment

Delete a feature flag segment.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await featureFlagsDeleteFlagSegment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
