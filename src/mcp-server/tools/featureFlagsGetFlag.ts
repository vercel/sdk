/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { featureFlagsGetFlag } from "../../funcs/featureFlagsGetFlag.js";
import { GetFlagRequest$inboundSchema } from "../../models/getflagop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetFlagRequest$inboundSchema,
};

export const tool$featureFlagsGetFlag: ToolDefinition<typeof args> = {
  name: "feature-flags-get-flag",
  description: `Get a flag

Retrieve a specific feature flag by its ID or slug.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await featureFlagsGetFlag(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
