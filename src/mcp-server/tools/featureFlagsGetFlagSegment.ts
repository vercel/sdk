/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { featureFlagsGetFlagSegment } from "../../funcs/featureFlagsGetFlagSegment.js";
import { GetFlagSegmentRequest$inboundSchema } from "../../models/getflagsegmentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetFlagSegmentRequest$inboundSchema,
};

export const tool$featureFlagsGetFlagSegment: ToolDefinition<typeof args> = {
  name: "feature-flags-get-flag-segment",
  description: `Get a segment

Retrieve a feature flag segment by ID or slug.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await featureFlagsGetFlagSegment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
