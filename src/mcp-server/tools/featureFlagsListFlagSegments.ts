/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { featureFlagsListFlagSegments } from "../../funcs/featureFlagsListFlagSegments.js";
import { ListFlagSegmentsRequest$inboundSchema } from "../../models/listflagsegmentsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListFlagSegmentsRequest$inboundSchema,
};

export const tool$featureFlagsListFlagSegments: ToolDefinition<typeof args> = {
  name: "feature-flags-list-flag-segments",
  description: `List segments

List all feature flag segments for a project.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await featureFlagsListFlagSegments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
