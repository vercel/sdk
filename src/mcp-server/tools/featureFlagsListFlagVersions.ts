/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { featureFlagsListFlagVersions } from "../../funcs/featureFlagsListFlagVersions.js";
import { ListFlagVersionsRequest$inboundSchema } from "../../models/listflagversionsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListFlagVersionsRequest$inboundSchema,
};

export const tool$featureFlagsListFlagVersions: ToolDefinition<typeof args> = {
  name: "feature-flags-list-flag-versions",
  description: `List flag versions

Lists flag versions for a given flag.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await featureFlagsListFlagVersions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
