/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { featureFlagsListFlagsV2 } from "../../funcs/featureFlagsListFlagsV2.js";
import { ListFlagsV2Request$inboundSchema } from "../../models/listflagsv2op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListFlagsV2Request$inboundSchema,
};

export const tool$featureFlagsListFlagsV2: ToolDefinition<typeof args> = {
  name: "feature-flags-list-flags-v2",
  description: `List flags

Retrieve feature flags for a project. Returns an opaque cursor for pagination.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await featureFlagsListFlagsV2(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
