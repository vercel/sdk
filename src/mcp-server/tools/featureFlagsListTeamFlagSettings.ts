/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { featureFlagsListTeamFlagSettings } from "../../funcs/featureFlagsListTeamFlagSettings.js";
import { ListTeamFlagSettingsRequest$inboundSchema } from "../../models/listteamflagsettingsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListTeamFlagSettingsRequest$inboundSchema,
};

export const tool$featureFlagsListTeamFlagSettings: ToolDefinition<
  typeof args
> = {
  name: "feature-flags-list-team-flag-settings",
  description: `List team project flag settings

Retrieve feature flag settings for projects in a team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await featureFlagsListTeamFlagSettings(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
