/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { featureFlagsListTeamFlags } from "../../funcs/featureFlagsListTeamFlags.js";
import { ListTeamFlagsRequest$inboundSchema } from "../../models/listteamflagsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListTeamFlagsRequest$inboundSchema,
};

export const tool$featureFlagsListTeamFlags: ToolDefinition<typeof args> = {
  name: "feature-flags-list-team-flags",
  description: `List all flags for a team

Retrieve all feature flags for a team across all projects. The list can be filtered by state and supports pagination.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await featureFlagsListTeamFlags(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
