/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { featureFlagsListTeamFlagsV2 } from "../../funcs/featureFlagsListTeamFlagsV2.js";
import { ListTeamFlagsV2Request$inboundSchema } from "../../models/listteamflagsv2op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListTeamFlagsV2Request$inboundSchema,
};

export const tool$featureFlagsListTeamFlagsV2: ToolDefinition<typeof args> = {
  name: "feature-flags-list-team-flags-v2",
  description: `List all flags for a team

Retrieve all feature flags for a team across all projects. Returns an opaque cursor for pagination.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await featureFlagsListTeamFlagsV2(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
