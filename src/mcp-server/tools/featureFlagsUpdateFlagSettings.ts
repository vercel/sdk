/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { featureFlagsUpdateFlagSettings } from "../../funcs/featureFlagsUpdateFlagSettings.js";
import { UpdateFlagSettingsRequest$inboundSchema } from "../../models/updateflagsettingsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateFlagSettingsRequest$inboundSchema,
};

export const tool$featureFlagsUpdateFlagSettings: ToolDefinition<typeof args> =
  {
    name: "feature-flags-update-flag-settings",
    description: `Update project flag settings

Update feature flag settings for a project.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await featureFlagsUpdateFlagSettings(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
