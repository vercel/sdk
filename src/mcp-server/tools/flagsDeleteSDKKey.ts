/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { flagsDeleteSDKKey } from "../../funcs/flagsDeleteSDKKey.js";
import { DeleteSDKKeyRequest$inboundSchema } from "../../models/deletesdkkeyop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteSDKKeyRequest$inboundSchema,
};

export const tool$flagsDeleteSDKKey: ToolDefinition<typeof args> = {
  name: "flags-delete-SDK-key",
  description: `Delete an SDK key

Deletes an SDK key.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await flagsDeleteSDKKey(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
