/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { flagsGetSDKKeys } from "../../funcs/flagsGetSDKKeys.js";
import { GetSDKKeysRequest$inboundSchema } from "../../models/getsdkkeysop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetSDKKeysRequest$inboundSchema,
};

export const tool$flagsGetSDKKeys: ToolDefinition<typeof args> = {
  name: "flags-get-SDK-keys",
  description: `Get all SDK keys

Gets all SDK keys for a project.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await flagsGetSDKKeys(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
