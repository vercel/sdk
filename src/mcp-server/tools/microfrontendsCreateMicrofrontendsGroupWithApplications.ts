/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { microfrontendsCreateMicrofrontendsGroupWithApplications } from "../../funcs/microfrontendsCreateMicrofrontendsGroupWithApplications.js";
import { CreateMicrofrontendsGroupWithApplicationsRequest$inboundSchema } from "../../models/createmicrofrontendsgroupwithapplicationsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateMicrofrontendsGroupWithApplicationsRequest$inboundSchema,
};

export const tool$microfrontendsCreateMicrofrontendsGroupWithApplications:
  ToolDefinition<typeof args> = {
    name: "microfrontends-create-microfrontends-group-with-applications",
    description: `Create a microfrontends group with applications

Creates a microfrontends group and attaches multiple projects in a single request.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await microfrontendsCreateMicrofrontendsGroupWithApplications(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
