/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { microfrontendsGetMicrofrontendsConfig } from "../../funcs/microfrontendsGetMicrofrontendsConfig.js";
import { GetMicrofrontendsConfigRequest$inboundSchema } from "../../models/getmicrofrontendsconfigop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetMicrofrontendsConfigRequest$inboundSchema,
};

export const tool$microfrontendsGetMicrofrontendsConfig: ToolDefinition<
  typeof args
> = {
  name: "microfrontends-get-microfrontends-config",
  description: `Get microfrontends config for a deployment

Get the microfrontends config for a deployment.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await microfrontendsGetMicrofrontendsConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
