/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { microfrontendsGetMicrofrontendsConfigForProject } from "../../funcs/microfrontendsGetMicrofrontendsConfigForProject.js";
import { GetMicrofrontendsConfigForProjectRequest$inboundSchema } from "../../models/getmicrofrontendsconfigforprojectop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetMicrofrontendsConfigForProjectRequest$inboundSchema,
};

export const tool$microfrontendsGetMicrofrontendsConfigForProject:
  ToolDefinition<typeof args> = {
    name: "microfrontends-get-microfrontends-config-for-project",
    description: `Get microfrontends config for a project

Get the microfrontends config for a project by ID or name.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await microfrontendsGetMicrofrontendsConfigForProject(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
