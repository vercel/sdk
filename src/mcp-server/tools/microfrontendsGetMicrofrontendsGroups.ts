/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { microfrontendsGetMicrofrontendsGroups } from "../../funcs/microfrontendsGetMicrofrontendsGroups.js";
import { GetMicrofrontendsGroupsRequest$inboundSchema } from "../../models/getmicrofrontendsgroupsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetMicrofrontendsGroupsRequest$inboundSchema,
};

export const tool$microfrontendsGetMicrofrontendsGroups: ToolDefinition<
  typeof args
> = {
  name: "microfrontends-get-microfrontends-groups",
  description: `List microfrontends groups

Get the microfrontends group IDs for a team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await microfrontendsGetMicrofrontendsGroups(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
