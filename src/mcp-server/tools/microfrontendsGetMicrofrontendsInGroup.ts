/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { microfrontendsGetMicrofrontendsInGroup } from "../../funcs/microfrontendsGetMicrofrontendsInGroup.js";
import { GetMicrofrontendsInGroupRequest$inboundSchema } from "../../models/getmicrofrontendsingroupflatratetier.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetMicrofrontendsInGroupRequest$inboundSchema,
};

export const tool$microfrontendsGetMicrofrontendsInGroup: ToolDefinition<
  typeof args
> = {
  name: "microfrontends-get-microfrontends-in-group",
  description: `List projects in a microfrontends group

Get the microfrontends for a given group ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await microfrontendsGetMicrofrontendsInGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
