/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { networkingDeleteNetwork } from "../../funcs/networkingDeleteNetwork.js";
import { DeleteNetworkRequest$inboundSchema } from "../../models/deletenetworkop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteNetworkRequest$inboundSchema,
};

export const tool$networkingDeleteNetwork: ToolDefinition<typeof args> = {
  name: "networking-delete-network",
  description: `Delete a Secure Compute network

Allows to delete a Secure Compute network.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await networkingDeleteNetwork(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
