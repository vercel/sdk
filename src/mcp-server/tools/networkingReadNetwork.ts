/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { networkingReadNetwork } from "../../funcs/networkingReadNetwork.js";
import { ReadNetworkRequest$inboundSchema } from "../../models/readnetworkop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ReadNetworkRequest$inboundSchema,
};

export const tool$networkingReadNetwork: ToolDefinition<typeof args> = {
  name: "networking-read-network",
  description: `Read a Secure Compute network

Allows to read a Secure Compute network.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await networkingReadNetwork(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
