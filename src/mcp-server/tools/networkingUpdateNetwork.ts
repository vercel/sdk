/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { networkingUpdateNetwork } from "../../funcs/networkingUpdateNetwork.js";
import { UpdateNetworkRequest$inboundSchema } from "../../models/updatenetworkop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateNetworkRequest$inboundSchema,
};

export const tool$networkingUpdateNetwork: ToolDefinition<typeof args> = {
  name: "networking-update-network",
  description: `Update a Secure Compute network

Allows to update a Secure Compute network.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await networkingUpdateNetwork(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
