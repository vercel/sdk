/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { networkingUpdateStaticIps } from "../../funcs/networkingUpdateStaticIps.js";
import { UpdateStaticIpsRequest$inboundSchema } from "../../models/updatestaticipsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateStaticIpsRequest$inboundSchema,
};

export const tool$networkingUpdateStaticIps: ToolDefinition<typeof args> = {
  name: "networking-update-static-ips",
  description: `Configures Static IPs for a project

Allows configuring Static IPs for a project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await networkingUpdateStaticIps(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
