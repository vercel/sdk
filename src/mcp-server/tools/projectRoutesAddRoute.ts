/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { projectRoutesAddRoute } from "../../funcs/projectRoutesAddRoute.js";
import { AddRouteRequest$inboundSchema } from "../../models/addrouteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AddRouteRequest$inboundSchema,
};

export const tool$projectRoutesAddRoute: ToolDefinition<typeof args> = {
  name: "project-routes-add-route",
  description: `Add a routing rule

Add a single routing rule to a project at a specified position. Defaults to the end of the list if no position is provided. The route is enabled by default. Stages a new version with the added route.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectRoutesAddRoute(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
