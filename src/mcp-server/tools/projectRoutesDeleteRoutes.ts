/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { projectRoutesDeleteRoutes } from "../../funcs/projectRoutesDeleteRoutes.js";
import { DeleteRoutesRequest$inboundSchema } from "../../models/deleteroutesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteRoutesRequest$inboundSchema,
};

export const tool$projectRoutesDeleteRoutes: ToolDefinition<typeof args> = {
  name: "project-routes-delete-routes",
  description: `Delete routing rules

Delete one or more routing rules from a project by ID. Stages a new version with the routes removed.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectRoutesDeleteRoutes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
