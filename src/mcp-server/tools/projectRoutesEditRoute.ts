/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { projectRoutesEditRoute } from "../../funcs/projectRoutesEditRoute.js";
import { EditRouteRequest$inboundSchema } from "../../models/editrouteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: EditRouteRequest$inboundSchema,
};

export const tool$projectRoutesEditRoute: ToolDefinition<typeof args> = {
  name: "project-routes-edit-route",
  description: `Edit a routing rule

Replace a routing rule identified by its ID, or restore it from the current production version. Stages a new version with the modified route.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectRoutesEditRoute(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
