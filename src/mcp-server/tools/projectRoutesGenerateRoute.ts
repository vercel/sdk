/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { projectRoutesGenerateRoute } from "../../funcs/projectRoutesGenerateRoute.js";
import { GenerateRouteRequest$inboundSchema } from "../../models/generaterouteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GenerateRouteRequest$inboundSchema,
};

export const tool$projectRoutesGenerateRoute: ToolDefinition<typeof args> = {
  name: "project-routes-generate-route",
  description: `Generate a routing rule from natural language

Generate a routing rule configuration from a natural language description. Returns a suggested route configuration that can be reviewed and saved.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectRoutesGenerateRoute(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
