/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { projectRoutesGetRouteVersions } from "../../funcs/projectRoutesGetRouteVersions.js";
import { GetRouteVersionsRequest$inboundSchema } from "../../models/getrouteversionsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetRouteVersionsRequest$inboundSchema,
};

export const tool$projectRoutesGetRouteVersions: ToolDefinition<typeof args> = {
  name: "project-routes-get-route-versions",
  description: `Get routing rule version history

Get the version history for a project's routing rules. Returns the staging version (if one exists) followed by production versions, most recent first. The staging version has \`isStaging: true\` and the current production version has \`isLive: true\`.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectRoutesGetRouteVersions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
