/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { projectRoutesGetRoutes } from "../../funcs/projectRoutesGetRoutes.js";
import { GetRoutesRequest$inboundSchema } from "../../models/getroutesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetRoutesRequest$inboundSchema,
};

export const tool$projectRoutesGetRoutes: ToolDefinition<typeof args> = {
  name: "project-routes-get-routes",
  description: `Get project routing rules

Get the routing rules for a project. Supports searching by name/ID/pattern, filtering by route type, and diffing staged changes against production.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectRoutesGetRoutes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
