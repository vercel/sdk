/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { projectRoutesStageRoutes } from "../../funcs/projectRoutesStageRoutes.js";
import { StageRoutesRequest$inboundSchema } from "../../models/stageroutesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: StageRoutesRequest$inboundSchema,
};

export const tool$projectRoutesStageRoutes: ToolDefinition<typeof args> = {
  name: "project-routes-stage-routes",
  description: `Stage routing rules

Stage routing rules for a project. Set \`overwrite\` to true to replace all existing rules, or omit it to merge with existing rules by ID. Returns the new staged version.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectRoutesStageRoutes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
