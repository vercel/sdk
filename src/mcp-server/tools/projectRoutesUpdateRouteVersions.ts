/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { projectRoutesUpdateRouteVersions } from "../../funcs/projectRoutesUpdateRouteVersions.js";
import { UpdateRouteVersionsRequest$inboundSchema } from "../../models/updaterouteversionsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateRouteVersionsRequest$inboundSchema,
};

export const tool$projectRoutesUpdateRouteVersions: ToolDefinition<
  typeof args
> = {
  name: "project-routes-update-route-versions",
  description: `Promote, restore, or discard a routing rule version

Promote staged routing rules to production, restore a previous production version, or discard staged changes. - \`promote\`: Publishes the staging version to production. - \`restore\`: Rolls back to a previous production version. - \`discard\`: Removes the staging version without publishing.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectRoutesUpdateRouteVersions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
