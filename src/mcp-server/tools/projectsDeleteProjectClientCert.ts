/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { projectsDeleteProjectClientCert } from "../../funcs/projectsDeleteProjectClientCert.js";
import { DeleteProjectClientCertRequest$inboundSchema } from "../../models/deleteprojectclientcertop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteProjectClientCertRequest$inboundSchema,
};

export const tool$projectsDeleteProjectClientCert: ToolDefinition<typeof args> =
  {
    name: "projects-delete-project-client-cert",
    description: `Delete client certificate for egress mTLS

Delete a client certificate for mTLS authentication to external origins.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await projectsDeleteProjectClientCert(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
