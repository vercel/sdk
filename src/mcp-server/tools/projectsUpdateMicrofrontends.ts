/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { projectsUpdateMicrofrontends } from "../../funcs/projectsUpdateMicrofrontends.js";
import { UpdateMicrofrontendsRequest$inboundSchema } from "../../models/updatemicrofrontendsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateMicrofrontendsRequest$inboundSchema,
};

export const tool$projectsUpdateMicrofrontends: ToolDefinition<typeof args> = {
  name: "projects-update-microfrontends",
  description: `Update the microfrontends settings

Update the microfrontends settings for a project.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectsUpdateMicrofrontends(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
