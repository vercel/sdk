/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesCreateDirectory } from "../../funcs/sandboxesCreateDirectory.js";
import { CreateDirectoryRequest$inboundSchema } from "../../models/createdirectoryop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateDirectoryRequest$inboundSchema,
};

export const tool$sandboxesCreateDirectory: ToolDefinition<typeof args> = {
  name: "sandboxes-create-directory",
  description: `Create a directory

Creates a new directory in a sandbox's filesystem. By default, parent directories are created recursively if they don't exist (similar to \`mkdir -p\`).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesCreateDirectory(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
