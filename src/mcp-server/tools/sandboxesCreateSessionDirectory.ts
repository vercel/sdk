/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesCreateSessionDirectory } from "../../funcs/sandboxesCreateSessionDirectory.js";
import { CreateSessionDirectoryRequest$inboundSchema } from "../../models/createsessiondirectoryop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateSessionDirectoryRequest$inboundSchema,
};

export const tool$sandboxesCreateSessionDirectory: ToolDefinition<typeof args> =
  {
    name: "sandboxes-create-session-directory",
    description: `Create a directory

Creates a new directory in a session's filesystem. By default, parent directories are created recursively if they don't exist (similar to \`mkdir -p\`).`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await sandboxesCreateSessionDirectory(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
