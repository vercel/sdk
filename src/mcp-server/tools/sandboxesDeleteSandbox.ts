/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesDeleteSandbox } from "../../funcs/sandboxesDeleteSandbox.js";
import { DeleteSandboxRequest$inboundSchema } from "../../models/deletesandboxop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteSandboxRequest$inboundSchema,
};

export const tool$sandboxesDeleteSandbox: ToolDefinition<typeof args> = {
  name: "sandboxes-delete-sandbox",
  description: `Delete a sandbox

Deletes a sandbox by name. If sandboxes are currently running, they will be stopped first. This operation deletes all sandbox entities with the given name and the named sandbox metadata.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesDeleteSandbox(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
