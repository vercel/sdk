/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesDeleteSessionSnapshot } from "../../funcs/sandboxesDeleteSessionSnapshot.js";
import { DeleteSessionSnapshotRequest$inboundSchema } from "../../models/deletesessionsnapshotop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteSessionSnapshotRequest$inboundSchema,
};

export const tool$sandboxesDeleteSessionSnapshot: ToolDefinition<typeof args> =
  {
    name: "sandboxes-delete-session-snapshot",
    description: `Delete a snapshot

Permanently deletes a snapshot and frees its associated storage. This action cannot be undone. After deletion, the snapshot can no longer be used to create new sessions.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await sandboxesDeleteSessionSnapshot(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
