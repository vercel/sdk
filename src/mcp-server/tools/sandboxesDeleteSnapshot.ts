/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesDeleteSnapshot } from "../../funcs/sandboxesDeleteSnapshot.js";
import { DeleteSnapshotRequest$inboundSchema } from "../../models/deletesnapshotop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteSnapshotRequest$inboundSchema,
};

export const tool$sandboxesDeleteSnapshot: ToolDefinition<typeof args> = {
  name: "sandboxes-delete-snapshot",
  description: `Delete a snapshot

Permanently deletes a snapshot and frees its associated storage. This action cannot be undone. After deletion, the snapshot can no longer be used to create new sandboxes.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesDeleteSnapshot(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
