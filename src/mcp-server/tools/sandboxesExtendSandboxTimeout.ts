/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesExtendSandboxTimeout } from "../../funcs/sandboxesExtendSandboxTimeout.js";
import { ExtendSandboxTimeoutRequest$inboundSchema } from "../../models/extendsandboxtimeoutop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ExtendSandboxTimeoutRequest$inboundSchema,
};

export const tool$sandboxesExtendSandboxTimeout: ToolDefinition<typeof args> = {
  name: "sandboxes-extend-sandbox-timeout",
  description: `Extend sandbox timeout

Extends the maximum execution time of a running sandbox. The sandbox must be active and able to accept commands. The total timeout cannot exceed the maximum allowed limit for your account.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesExtendSandboxTimeout(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
