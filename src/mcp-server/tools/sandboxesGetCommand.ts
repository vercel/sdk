/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesGetCommand } from "../../funcs/sandboxesGetCommand.js";
import { GetCommandRequest$inboundSchema } from "../../models/getcommandop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetCommandRequest$inboundSchema,
};

export const tool$sandboxesGetCommand: ToolDefinition<typeof args> = {
  name: "sandboxes-get-command",
  description: `Get a command

Retrieves the current status and details of a command executed in a sandbox. Use the \`wait\` parameter to block until the command finishes execution.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesGetCommand(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
