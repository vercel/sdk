/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesGetCommandLogs } from "../../funcs/sandboxesGetCommandLogs.js";
import { GetCommandLogsRequest$inboundSchema } from "../../models/getcommandlogsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetCommandLogsRequest$inboundSchema,
};

export const tool$sandboxesGetCommandLogs: ToolDefinition<typeof args> = {
  name: "sandboxes-get-command-logs",
  description: `Stream command logs

Streams the output of a command in real-time using newline-delimited JSON (ND-JSON). Each entry includes the output data and stream type. Stream types include \`stdout\`, \`stderr\`, and \`error\` (for stream failures).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesGetCommandLogs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
