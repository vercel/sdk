/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesGetOrCreateDrive } from "../../funcs/sandboxesGetOrCreateDrive.js";
import { GetOrCreateDriveRequest$inboundSchema } from "../../models/getorcreatedriveop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetOrCreateDriveRequest$inboundSchema,
};

export const tool$sandboxesGetOrCreateDrive: ToolDefinition<typeof args> = {
  name: "sandboxes-get-or-create-drive",
  description: `Get or create a drive

Gets an existing drive by project and name, or creates it when it does not exist. Drives are in private beta. Register your interest to get access: https://vercel.com/changelog/drives-for-vercel-sandbox-in-private-beta`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesGetOrCreateDrive(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
