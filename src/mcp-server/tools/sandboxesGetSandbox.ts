/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesGetSandbox } from "../../funcs/sandboxesGetSandbox.js";
import { GetSandboxRequest$inboundSchema } from "../../models/getsandboxop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetSandboxRequest$inboundSchema,
};

export const tool$sandboxesGetSandbox: ToolDefinition<typeof args> = {
  name: "sandboxes-get-sandbox",
  description: `Get a sandbox

Retrieves detailed information about a specific sandbox, including its current status, resource configuration, and exposed routes.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesGetSandbox(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
