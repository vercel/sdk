/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesGetSandboxesV1 } from "../../funcs/sandboxesGetSandboxesV1.js";
import { GetSandboxesV1Request$inboundSchema } from "../../models/getsandboxesv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetSandboxesV1Request$inboundSchema,
};

export const tool$sandboxesGetSandboxesV1: ToolDefinition<typeof args> = {
  name: "sandboxes-get-sandboxes-v1",
  description: `List sandboxes

Retrieves a paginated list of sandboxes belonging to a specific project. Results can be filtered by creation time using the \`since\` and \`until\` parameters.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesGetSandboxesV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
