/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesGetSessionSnapshot } from "../../funcs/sandboxesGetSessionSnapshot.js";
import { GetSessionSnapshotRequest$inboundSchema } from "../../models/getsessionsnapshotop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetSessionSnapshotRequest$inboundSchema,
};

export const tool$sandboxesGetSessionSnapshot: ToolDefinition<typeof args> = {
  name: "sandboxes-get-session-snapshot",
  description: `Get a snapshot

Retrieves detailed information about a specific snapshot, including its creation time, size, expiration date, and the source session it was created from.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesGetSessionSnapshot(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
