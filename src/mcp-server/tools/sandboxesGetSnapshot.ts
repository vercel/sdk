/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesGetSnapshot } from "../../funcs/sandboxesGetSnapshot.js";
import { GetSnapshotRequest$inboundSchema } from "../../models/getsnapshotop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetSnapshotRequest$inboundSchema,
};

export const tool$sandboxesGetSnapshot: ToolDefinition<typeof args> = {
  name: "sandboxes-get-snapshot",
  description: `Get a snapshot

Retrieves detailed information about a specific snapshot, including its creation time, size, expiration date, and the source sandbox it was created from.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesGetSnapshot(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
