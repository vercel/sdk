/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesListCommands } from "../../funcs/sandboxesListCommands.js";
import { ListCommandsRequest$inboundSchema } from "../../models/listcommandsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListCommandsRequest$inboundSchema,
};

export const tool$sandboxesListCommands: ToolDefinition<typeof args> = {
  name: "sandboxes-list-commands",
  description: `List commands

Retrieves a list of all commands that have been executed in a sandbox, including their current status, exit codes, and execution times.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesListCommands(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
