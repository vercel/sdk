/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesListDrives } from "../../funcs/sandboxesListDrives.js";
import { ListDrivesRequest$inboundSchema } from "../../models/listdrivesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListDrivesRequest$inboundSchema,
};

export const tool$sandboxesListDrives: ToolDefinition<typeof args> = {
  name: "sandboxes-list-drives",
  description: `List drives

Retrieves a paginated list of drives belonging to a specific project. Drives are in private beta. Register your interest to get access: https://vercel.com/changelog/drives-for-vercel-sandbox-in-private-beta`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesListDrives(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
