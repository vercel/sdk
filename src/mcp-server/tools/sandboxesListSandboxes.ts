/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesListSandboxes } from "../../funcs/sandboxesListSandboxes.js";
import { ListSandboxesRequest$inboundSchema } from "../../models/listsandboxesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListSandboxesRequest$inboundSchema,
};

export const tool$sandboxesListSandboxes: ToolDefinition<typeof args> = {
  name: "sandboxes-list-sandboxes",
  description: `List sandboxes

Retrieves a paginated list of named sandboxes belonging to a specific project. Results can be sorted by creation time or name, and optionally filtered by name prefix.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesListSandboxes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
