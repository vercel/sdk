/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesListSessionCommands } from "../../funcs/sandboxesListSessionCommands.js";
import { ListSessionCommandsRequest$inboundSchema } from "../../models/listsessioncommandsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListSessionCommandsRequest$inboundSchema,
};

export const tool$sandboxesListSessionCommands: ToolDefinition<typeof args> = {
  name: "sandboxes-list-session-commands",
  description: `List commands

Retrieves a list of all commands that have been executed in a session, including their current status, exit codes, and execution times, ordered from the most recent to the oldest.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesListSessionCommands(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
