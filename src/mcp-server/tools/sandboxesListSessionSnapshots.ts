/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesListSessionSnapshots } from "../../funcs/sandboxesListSessionSnapshots.js";
import { ListSessionSnapshotsRequest$inboundSchema } from "../../models/listsessionsnapshotsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListSessionSnapshotsRequest$inboundSchema,
};

export const tool$sandboxesListSessionSnapshots: ToolDefinition<typeof args> = {
  name: "sandboxes-list-session-snapshots",
  description: `List snapshots

Retrieves a paginated list of snapshots for a specific project.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesListSessionSnapshots(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
