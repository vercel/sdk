/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesListSnapshots } from "../../funcs/sandboxesListSnapshots.js";
import { ListSnapshotsRequest$inboundSchema } from "../../models/listsnapshotsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListSnapshotsRequest$inboundSchema,
};

export const tool$sandboxesListSnapshots: ToolDefinition<typeof args> = {
  name: "sandboxes-list-snapshots",
  description: `List snapshots

Retrieves a paginated list of snapshots for a specific project.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesListSnapshots(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
