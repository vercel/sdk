/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesReadFile } from "../../funcs/sandboxesReadFile.js";
import { ReadFileRequest$inboundSchema } from "../../models/readfileop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ReadFileRequest$inboundSchema,
};

export const tool$sandboxesReadFile: ToolDefinition<typeof args> = {
  name: "sandboxes-read-file",
  description: `Read a file

Downloads the contents of a file from a sandbox's filesystem. The file content is returned as a binary stream with appropriate Content-Disposition headers for file download.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesReadFile(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
