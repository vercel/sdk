/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesReadSessionFile } from "../../funcs/sandboxesReadSessionFile.js";
import { ReadSessionFileRequest$inboundSchema } from "../../models/readsessionfileop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ReadSessionFileRequest$inboundSchema,
};

export const tool$sandboxesReadSessionFile: ToolDefinition<typeof args> = {
  name: "sandboxes-read-session-file",
  description: `Read a file

Downloads the contents of a file from a session's filesystem. The file content is returned as a binary stream with appropriate Content-Disposition headers for file download.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesReadSessionFile(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
