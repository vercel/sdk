/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesStopSandbox } from "../../funcs/sandboxesStopSandbox.js";
import { StopSandboxRequest$inboundSchema } from "../../models/stopsandboxop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: StopSandboxRequest$inboundSchema,
};

export const tool$sandboxesStopSandbox: ToolDefinition<typeof args> = {
  name: "sandboxes-stop-sandbox",
  description: `Stop a sandbox

Stops a running sandbox and releases its allocated resources. All running processes within the sandbox will be terminated. This action cannot be undone. A stopped sandbox cannot be restarted.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesStopSandbox(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
