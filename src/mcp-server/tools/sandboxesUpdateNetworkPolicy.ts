/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesUpdateNetworkPolicy } from "../../funcs/sandboxesUpdateNetworkPolicy.js";
import { UpdateNetworkPolicyRequest$inboundSchema } from "../../models/updatenetworkpolicyop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateNetworkPolicyRequest$inboundSchema,
};

export const tool$sandboxesUpdateNetworkPolicy: ToolDefinition<typeof args> = {
  name: "sandboxes-update-network-policy",
  description: `Update network policy

Replaces the network access policy of a running sandbox. Use this to control which external hosts the sandbox can communicate with. This is a full replacement. Any previously configured network rules will be overwritten.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesUpdateNetworkPolicy(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
