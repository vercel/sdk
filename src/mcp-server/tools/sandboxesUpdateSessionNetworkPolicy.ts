/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesUpdateSessionNetworkPolicy } from "../../funcs/sandboxesUpdateSessionNetworkPolicy.js";
import { UpdateSessionNetworkPolicyRequest$inboundSchema } from "../../models/updatesessionnetworkpolicyop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateSessionNetworkPolicyRequest$inboundSchema,
};

export const tool$sandboxesUpdateSessionNetworkPolicy: ToolDefinition<
  typeof args
> = {
  name: "sandboxes-update-session-network-policy",
  description: `Update network policy

Replaces the network access policy of a running session. Use this to control which external hosts the session can communicate with. This is a full replacement. Any previously configured network rules will be overwritten.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesUpdateSessionNetworkPolicy(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
