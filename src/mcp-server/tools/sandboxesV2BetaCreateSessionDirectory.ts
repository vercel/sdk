/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesV2BetaCreateSessionDirectory } from "../../funcs/sandboxesV2BetaCreateSessionDirectory.js";
import { CreateSessionDirectoryRequest$inboundSchema } from "../../models/createsessiondirectoryop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateSessionDirectoryRequest$inboundSchema,
};

export const tool$sandboxesV2BetaCreateSessionDirectory: ToolDefinition<
  typeof args
> = {
  name: "sandboxes-v2-beta-create-session-directory",
  description: `Create a directory

Creates a new directory in a session's filesystem. By default, parent directories are created recursively if they don't exist (similar to \`mkdir -p\`).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesV2BetaCreateSessionDirectory(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
