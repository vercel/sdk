/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesV2BetaCreateSessionSnapshot } from "../../funcs/sandboxesV2BetaCreateSessionSnapshot.js";
import { CreateSessionSnapshotRequest$inboundSchema } from "../../models/createsessionsnapshotop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateSessionSnapshotRequest$inboundSchema,
};

export const tool$sandboxesV2BetaCreateSessionSnapshot: ToolDefinition<
  typeof args
> = {
  name: "sandboxes-v2-beta-create-session-snapshot",
  description: `Create a snapshot

Creates a point-in-time snapshot of a running session's filesystem. Snapshots can be used to quickly restore a session to a previous state or to create new sessions with pre-configured environments. The session must be running and able to accept commands for a snapshot to be created. The session will be terminated after the snapshot is created.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesV2BetaCreateSessionSnapshot(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
