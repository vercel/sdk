/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesV2BetaDeleteSandbox } from "../../funcs/sandboxesV2BetaDeleteSandbox.js";
import { DeleteSandboxRequest$inboundSchema } from "../../models/deletesandboxop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteSandboxRequest$inboundSchema,
};

export const tool$sandboxesV2BetaDeleteSandbox: ToolDefinition<typeof args> = {
  name: "sandboxes-v2-beta-delete-sandbox",
  description: `Delete a sandbox

Deletes a sandbox by name. If sandboxes are currently running, they will be stopped first. This operation deletes all sandbox entities with the given name, all associated snapshots, and the named sandbox metadata.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesV2BetaDeleteSandbox(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
