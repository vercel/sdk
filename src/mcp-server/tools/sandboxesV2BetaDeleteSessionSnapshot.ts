/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesV2BetaDeleteSessionSnapshot } from "../../funcs/sandboxesV2BetaDeleteSessionSnapshot.js";
import { DeleteSessionSnapshotRequest$inboundSchema } from "../../models/deletesessionsnapshotop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteSessionSnapshotRequest$inboundSchema,
};

export const tool$sandboxesV2BetaDeleteSessionSnapshot: ToolDefinition<
  typeof args
> = {
  name: "sandboxes-v2-beta-delete-session-snapshot",
  description: `Delete a snapshot

Permanently deletes a snapshot and frees its associated storage. This action cannot be undone. After deletion, the snapshot can no longer be used to create new sessions.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesV2BetaDeleteSessionSnapshot(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
