/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesV2BetaExtendSessionTimeout } from "../../funcs/sandboxesV2BetaExtendSessionTimeout.js";
import { ExtendSessionTimeoutRequest$inboundSchema } from "../../models/extendsessiontimeoutop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ExtendSessionTimeoutRequest$inboundSchema,
};

export const tool$sandboxesV2BetaExtendSessionTimeout: ToolDefinition<
  typeof args
> = {
  name: "sandboxes-v2-beta-extend-session-timeout",
  description: `Extend session timeout

Extends the maximum execution time of a running session. The session must be active and able to accept commands. The total timeout cannot exceed the maximum allowed limit for your account.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesV2BetaExtendSessionTimeout(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
