/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesV2BetaGetNamedSandbox } from "../../funcs/sandboxesV2BetaGetNamedSandbox.js";
import { GetNamedSandboxRequest$inboundSchema } from "../../models/getnamedsandboxop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetNamedSandboxRequest$inboundSchema,
};

export const tool$sandboxesV2BetaGetNamedSandbox: ToolDefinition<typeof args> =
  {
    name: "sandboxes-v2-beta-get-named-sandbox",
    description: `Get a named sandbox

Retrieves a named sandbox by name, including its current sandbox and routes. If the sandbox is stopped and resume is true, a new sandbox will be created from the most recent snapshot.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await sandboxesV2BetaGetNamedSandbox(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
