/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesV2BetaGetSandboxesV2 } from "../../funcs/sandboxesV2BetaGetSandboxesV2.js";
import { GetSandboxesV2Request$inboundSchema } from "../../models/getsandboxesv2op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetSandboxesV2Request$inboundSchema,
};

export const tool$sandboxesV2BetaGetSandboxesV2: ToolDefinition<typeof args> = {
  name: "sandboxes-v2-beta-get-sandboxes-v2",
  description: `List sandboxes

Retrieves a paginated list of named sandboxes belonging to a specific project. Results can be sorted by creation time or name, and optionally filtered by name prefix.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesV2BetaGetSandboxesV2(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
