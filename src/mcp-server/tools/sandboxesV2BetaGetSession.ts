/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesV2BetaGetSession } from "../../funcs/sandboxesV2BetaGetSession.js";
import { GetSessionRequest$inboundSchema } from "../../models/getsessionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetSessionRequest$inboundSchema,
};

export const tool$sandboxesV2BetaGetSession: ToolDefinition<typeof args> = {
  name: "sandboxes-v2-beta-get-session",
  description: `Get a session

Retrieves detailed information about a specific session, including its current status, resource configuration, and exposed routes.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesV2BetaGetSession(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
