/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesV2BetaGetSessionCommand } from "../../funcs/sandboxesV2BetaGetSessionCommand.js";
import { GetSessionCommandRequest$inboundSchema } from "../../models/getsessioncommandop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetSessionCommandRequest$inboundSchema,
};

export const tool$sandboxesV2BetaGetSessionCommand: ToolDefinition<
  typeof args
> = {
  name: "sandboxes-v2-beta-get-session-command",
  description: `Get a command

Retrieves the current status and details of a command executed in a session. Use the \`wait\` parameter to block until the command finishes execution.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesV2BetaGetSessionCommand(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
