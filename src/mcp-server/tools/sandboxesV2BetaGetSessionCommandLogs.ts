/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesV2BetaGetSessionCommandLogs } from "../../funcs/sandboxesV2BetaGetSessionCommandLogs.js";
import { GetSessionCommandLogsRequest$inboundSchema } from "../../models/getsessioncommandlogsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetSessionCommandLogsRequest$inboundSchema,
};

export const tool$sandboxesV2BetaGetSessionCommandLogs: ToolDefinition<
  typeof args
> = {
  name: "sandboxes-v2-beta-get-session-command-logs",
  description: `Stream command logs

Streams the output of a command in real-time using newline-delimited JSON (ND-JSON). Each entry includes the output data and stream type. Stream types include \`stdout\`, \`stderr\`, and \`error\` (for stream failures).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesV2BetaGetSessionCommandLogs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
