/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesV2BetaGetSessionSnapshot } from "../../funcs/sandboxesV2BetaGetSessionSnapshot.js";
import { GetSessionSnapshotRequest$inboundSchema } from "../../models/getsessionsnapshotop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetSessionSnapshotRequest$inboundSchema,
};

export const tool$sandboxesV2BetaGetSessionSnapshot: ToolDefinition<
  typeof args
> = {
  name: "sandboxes-v2-beta-get-session-snapshot",
  description: `Get a snapshot

Retrieves detailed information about a specific snapshot, including its creation time, size, expiration date, and the source session it was created from.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesV2BetaGetSessionSnapshot(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
