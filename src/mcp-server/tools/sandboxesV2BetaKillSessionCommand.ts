/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesV2BetaKillSessionCommand } from "../../funcs/sandboxesV2BetaKillSessionCommand.js";
import { KillSessionCommandRequest$inboundSchema } from "../../models/killsessioncommandop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: KillSessionCommandRequest$inboundSchema,
};

export const tool$sandboxesV2BetaKillSessionCommand: ToolDefinition<
  typeof args
> = {
  name: "sandboxes-v2-beta-kill-session-command",
  description: `Kill a command

Sends a signal to terminate a running command in a session. The signal can be used to gracefully stop (SIGTERM) or forcefully kill (SIGKILL) the process. The command must still be running for this operation to succeed.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesV2BetaKillSessionCommand(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
