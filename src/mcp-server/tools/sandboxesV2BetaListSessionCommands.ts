/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesV2BetaListSessionCommands } from "../../funcs/sandboxesV2BetaListSessionCommands.js";
import { ListSessionCommandsRequest$inboundSchema } from "../../models/listsessioncommandsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListSessionCommandsRequest$inboundSchema,
};

export const tool$sandboxesV2BetaListSessionCommands: ToolDefinition<
  typeof args
> = {
  name: "sandboxes-v2-beta-list-session-commands",
  description: `List commands

Retrieves a list of all commands that have been executed in a session, including their current status, exit codes, and execution times, ordered from the most recent to the oldest.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesV2BetaListSessionCommands(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
