/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesV2BetaListSessions } from "../../funcs/sandboxesV2BetaListSessions.js";
import { ListSessionsRequest$inboundSchema } from "../../models/listsessionsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListSessionsRequest$inboundSchema,
};

export const tool$sandboxesV2BetaListSessions: ToolDefinition<typeof args> = {
  name: "sandboxes-v2-beta-list-sessions",
  description: `List sessions

Retrieves a paginated list of sessions belonging to a specific sandbox. Results are sorted by creation time and paginated using an opaque cursor.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesV2BetaListSessions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
