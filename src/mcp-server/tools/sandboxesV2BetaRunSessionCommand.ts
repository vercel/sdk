/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesV2BetaRunSessionCommand } from "../../funcs/sandboxesV2BetaRunSessionCommand.js";
import { RunSessionCommandRequest$inboundSchema } from "../../models/runsessioncommandop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RunSessionCommandRequest$inboundSchema,
};

export const tool$sandboxesV2BetaRunSessionCommand: ToolDefinition<
  typeof args
> = {
  name: "sandboxes-v2-beta-run-session-command",
  description: `Execute a command

Executes a shell command inside a running session. The command runs asynchronously and returns immediately with a command ID that can be used to track its progress and retrieve its output. Optionally, use the \`wait\` parameter to stream the command status until completion.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesV2BetaRunSessionCommand(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
