/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesV2BetaStopSession } from "../../funcs/sandboxesV2BetaStopSession.js";
import { StopSessionRequest$inboundSchema } from "../../models/stopsessionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: StopSessionRequest$inboundSchema,
};

export const tool$sandboxesV2BetaStopSession: ToolDefinition<typeof args> = {
  name: "sandboxes-v2-beta-stop-session",
  description: `Stop a session

Stops a running session and releases its allocated resources. All running processes within the session will be terminated. This action cannot be undone. A stopped session cannot be restarted.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesV2BetaStopSession(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
