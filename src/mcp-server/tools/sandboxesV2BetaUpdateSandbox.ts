/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesV2BetaUpdateSandbox } from "../../funcs/sandboxesV2BetaUpdateSandbox.js";
import { UpdateSandboxRequest$inboundSchema } from "../../models/updatesandboxop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateSandboxRequest$inboundSchema,
};

export const tool$sandboxesV2BetaUpdateSandbox: ToolDefinition<typeof args> = {
  name: "sandboxes-v2-beta-update-sandbox",
  description: `Update a sandbox

Updates the configuration of a sandbox. Only the provided fields will be modified; omitted fields remain unchanged.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesV2BetaUpdateSandbox(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
