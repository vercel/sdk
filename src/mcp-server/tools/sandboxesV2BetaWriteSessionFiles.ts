/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sandboxesV2BetaWriteSessionFiles } from "../../funcs/sandboxesV2BetaWriteSessionFiles.js";
import { WriteSessionFilesRequest$inboundSchema } from "../../models/writesessionfilesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: WriteSessionFilesRequest$inboundSchema,
};

export const tool$sandboxesV2BetaWriteSessionFiles: ToolDefinition<
  typeof args
> = {
  name: "sandboxes-v2-beta-write-session-files",
  description: `Write files

Uploads and extracts files to a session's filesystem. Files must be uploaded as a gzipped tarball (\`.tar.gz\`) with the \`Content-Type\` header set to \`application/gzip\`. The tarball contents are extracted to the session's working directory, or to a custom directory specified via the \`x-cwd\` header.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sandboxesV2BetaWriteSessionFiles(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
