/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teamsDeleteMicrofrontendsGroup } from "../../funcs/teamsDeleteMicrofrontendsGroup.js";
import { DeleteMicrofrontendsGroupRequest$inboundSchema } from "../../models/deletemicrofrontendsgroupop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteMicrofrontendsGroupRequest$inboundSchema,
};

export const tool$teamsDeleteMicrofrontendsGroup: ToolDefinition<typeof args> =
  {
    name: "teams-delete-microfrontends-group",
    description: `Delete a microfrontends group

Deletes a microfrontends group from the team associated with the group ID.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await teamsDeleteMicrofrontendsGroup(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
