/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { userListEventTypes } from "../../funcs/userListEventTypes.js";
import { ListEventTypesRequest$inboundSchema } from "../../models/listeventtypesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListEventTypesRequest$inboundSchema,
};

export const tool$userListEventTypes: ToolDefinition<typeof args> = {
  name: "user-list-event-types",
  description: `List Event Types

Returns the list of user-facing event types with descriptions.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await userListEventTypes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
