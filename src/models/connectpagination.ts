/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./sdkvalidationerror.js";

/**
 * Cursor for the next page.
 */
export type ConnectPagination = {
  /**
   * Opaque value to pass as `cursor` on the next request.
   */
  next: string | null;
};

/** @internal */
export const ConnectPagination$inboundSchema: z.ZodType<
  ConnectPagination,
  z.ZodTypeDef,
  unknown
> = z.object({
  next: types.nullable(types.string()),
});

export function connectPaginationFromJSON(
  jsonString: string,
): SafeParseResult<ConnectPagination, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ConnectPagination$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ConnectPagination' from JSON`,
  );
}
