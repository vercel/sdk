/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./sdkvalidationerror.js";

/**
 * Incoming trigger configuration. Only present when enabled.
 */
export type ConnectTriggerConfiguration = {
  /**
   * Whether incoming triggers are enabled for the connector.
   */
  enabled: boolean;
};

/** @internal */
export const ConnectTriggerConfiguration$inboundSchema: z.ZodType<
  ConnectTriggerConfiguration,
  z.ZodTypeDef,
  unknown
> = z.object({
  enabled: types.boolean(),
});

export function connectTriggerConfigurationFromJSON(
  jsonString: string,
): SafeParseResult<ConnectTriggerConfiguration, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ConnectTriggerConfiguration$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ConnectTriggerConfiguration' from JSON`,
  );
}
