/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { smartUnion } from "../types/smartUnion.js";
import {
  ConnectConnectorCreateResult,
  ConnectConnectorCreateResult$inboundSchema,
} from "./connectconnectorcreateresult.js";
import {
  ConnectCreateConnectorRequest,
  ConnectCreateConnectorRequest$Outbound,
  ConnectCreateConnectorRequest$outboundSchema,
} from "./connectcreateconnectorrequest.js";
import {
  ConnectTriggerConfiguration,
  ConnectTriggerConfiguration$inboundSchema,
} from "./connecttriggerconfiguration.js";
import {
  ConnectTriggerDestination,
  ConnectTriggerDestination$inboundSchema,
} from "./connecttriggerdestination.js";
import { SDKValidationError } from "./sdkvalidationerror.js";

export type CreateConnectorRequest = {
  /**
   * The team ID that scopes the request. Do not send it with slug. If both are omitted, Vercel uses the team associated with the token or the authenticated user's default team. The request returns 401 if no team can be selected.
   */
  teamId?: string | undefined;
  /**
   * The team slug that scopes the request. Do not send it with teamId. If both are omitted, Vercel uses the team associated with the token or the authenticated user's default team. The request returns 401 if no team can be selected.
   */
  slug?: string | undefined;
  connectCreateConnectorRequest?: ConnectCreateConnectorRequest | undefined;
};

export const Environment2 = {
  Development: "development",
  Preview: "preview",
  Production: "production",
} as const;
export type Environment2 = ClosedEnum<typeof Environment2>;

export type CreateConnectorCreatedByEnvironment = string | Environment2;

/**
 * Principal that originally created the connector — either a Vercel user (interactive dashboard / CLI flow) or a Vercel deployment (OIDC-authenticated project, used by runtime auto-provisioning). See {@link ConnexPrincipal}. Optional: pre-existing rows from before this shape was introduced may carry no attribution at all.
 */
export type CreatedBy2 = {
  /**
   * Principal kind.
   */
  type: "project";
  /**
   * Vercel project ID.
   */
  id: string;
  environment: string | Environment2;
};

/**
 * Principal that originally created the connector — either a Vercel user (interactive dashboard / CLI flow) or a Vercel deployment (OIDC-authenticated project, used by runtime auto-provisioning). See {@link ConnexPrincipal}. Optional: pre-existing rows from before this shape was introduced may carry no attribution at all.
 */
export type CreatedBy1 = {
  /**
   * Principal kind.
   */
  type: "user";
  /**
   * Vercel user ID.
   */
  id: string;
};

export type CreateConnectorCreatedBy = CreatedBy1 | CreatedBy2;

export const CreateConnectorEnvironment2 = {
  Development: "development",
  Preview: "preview",
  Production: "production",
} as const;
export type CreateConnectorEnvironment2 = ClosedEnum<
  typeof CreateConnectorEnvironment2
>;

export type CreateConnectorUpdatedByEnvironment =
  | string
  | CreateConnectorEnvironment2;

/**
 * Principal that most recently mutated the connector. Same shape as {@link createdBy} but tracks the most recent updater, not the original creator. At create time the two fields point at the same principal; they diverge on the first subsequent update.
 */
export type CreateConnectorUpdatedBy2 = {
  /**
   * Principal kind.
   */
  type: "project";
  /**
   * Vercel project ID.
   */
  id: string;
  environment: string | CreateConnectorEnvironment2;
};

/**
 * Principal that most recently mutated the connector. Same shape as {@link createdBy} but tracks the most recent updater, not the original creator. At create time the two fields point at the same principal; they diverge on the first subsequent update.
 */
export type CreateConnectorUpdatedBy1 = {
  /**
   * Principal kind.
   */
  type: "user";
  /**
   * Vercel user ID.
   */
  id: string;
};

export type CreateConnectorUpdatedBy =
  | CreateConnectorUpdatedBy1
  | CreateConnectorUpdatedBy2;

/**
 * How the connector row was originally created. New create paths stamp this explicitly; older rows may omit it.
 */
export const CreateConnectorCreationMode = {
  Managed: "managed",
  Manual: "manual",
} as const;
/**
 * How the connector row was originally created. New create paths stamp this explicitly; older rows may omit it.
 */
export type CreateConnectorCreationMode = ClosedEnum<
  typeof CreateConnectorCreationMode
>;

/**
 * Managed connector metadata exposed without leaking the manager connector or installation identifiers.
 */
export type CreateConnectorManaged = {
  /**
   * Whether Vercel synchronizes provider-side configuration.
   */
  sync?: boolean | undefined;
};

/**
 * Connector implementation type.
 */
export const CreateConnectorType = {
  ApiKey: "api-key",
  Custom: "custom",
  Discord: "discord",
  Github: "github",
  Linear: "linear",
  Linq: "linq",
  MicrosoftEntra: "microsoft-entra",
  Oauth: "oauth",
  Photon: "photon",
  Salesforce: "salesforce",
  Sendblue: "sendblue",
  Slack: "slack",
  Snowflake: "snowflake",
  SnowflakeWif: "snowflake-wif",
} as const;
/**
 * Connector implementation type.
 */
export type CreateConnectorType = ClosedEnum<typeof CreateConnectorType>;

export type CreateConnectorAppTokens = {
  /**
   * Whether one app token can be used across installations.
   */
  crossInstallation: boolean;
  /**
   * Whether callers can narrow app-token grants per request.
   */
  supportsRefinement: boolean;
  /**
   * True when changing app token grants requires reinstalling the app, so tokens cannot be partitioned independently by requester environment.
   */
  requiresReinstallation?: boolean | undefined;
  /**
   * Known allowed app-level scopes. For Slack this is the bot scope set configured on the app; for OAuth it is the connector's enabled `clientCredentials.scopes` configuration.
   */
  scopes?: Array<string> | undefined;
  /**
   * Supported OAuth authorization-detail type names.
   */
  supportedAuthorizationDetails?: Array<string> | undefined;
  /**
   * Link to the page on the service where this connector's app-level permissions are declared and granted, when the service has one and it differs from `clientUrl`.
   */
  permissionsUrl?: string | undefined;
};

export type CreateConnectorUserTokens = {
  /**
   * Whether one user token can be used across installations.
   */
  crossInstallation: boolean;
  /**
   * Whether callers can narrow user-token grants per request.
   */
  supportsRefinement: boolean;
  /**
   * Known allowed user-level scopes. For Slack this is the user scope set configured on the app; for OAuth it is the connector's enabled `userAuthorization.scopes` configuration.
   */
  scopes?: Array<string> | undefined;
  /**
   * Supported OAuth authorization-detail type names.
   */
  supportedAuthorizationDetails?: Array<string> | undefined;
  /**
   * User authorization is completed by the Connect consent screen submitting a credential instead of an OAuth redirect.
   */
  manualCredentialInput?: boolean | undefined;
};

export const CreateConnectorSupportsIcon = {
  False: "false",
  Maybe: "maybe",
  True: "true",
} as const;
export type CreateConnectorSupportsIcon = ClosedEnum<
  typeof CreateConnectorSupportsIcon
>;

/**
 * Custom environments available on the project. This list can include environments where the connector is not enabled.
 */
export type CreateConnectorCustomEnvironments = {
  /**
   * Stable custom environment ID.
   */
  id: string;
  /**
   * Current human-readable custom environment slug.
   */
  slug: string;
};

/**
 * Project metadata. `project.id` contains the same value as `projectId`.
 */
export type CreateConnectorProject = {
  /**
   * Same Vercel project ID as the connection's top-level `projectId`.
   */
  id: string;
  /**
   * Current Vercel project name.
   */
  name: string;
  /**
   * Custom environments available on the project. This list can include environments where the connector is not enabled.
   */
  customEnvironments?: Array<CreateConnectorCustomEnvironments> | undefined;
};

export const CreateConnectorEnvironments2 = {
  Development: "development",
  Preview: "preview",
  Production: "production",
} as const;
export type CreateConnectorEnvironments2 = ClosedEnum<
  typeof CreateConnectorEnvironments2
>;

export type CreateConnectorEnvironments = string | CreateConnectorEnvironments2;

export type CreateConnectorItems = {
  clientId: string;
  /**
   * Project metadata. `project.id` contains the same value as `projectId`.
   */
  project?: CreateConnectorProject | undefined;
  /**
   * Environments where this connector is enabled for the project.
   */
  environments: Array<string | CreateConnectorEnvironments2>;
  /**
   * Time when the project connection was created, in epoch milliseconds.
   */
  createdAt: number;
  /**
   * Time when the project connection was last updated, in epoch milliseconds.
   */
  updatedAt: number;
};

export type CreateConnectorPagination = {
  next: string | null;
};

/**
 * Set by `?include=projects`. Capped at 100 per client.
 */
export type CreateConnectorProjects = {
  items: Array<CreateConnectorItems>;
  hasMore: boolean;
  pagination: CreateConnectorPagination;
};

/**
 * Optional expansions populated by `?include=...` on the list endpoint.
 */
export type Includes = {
  /**
   * Set by `?include=projects`. Capped at 100 per client.
   */
  projects?: CreateConnectorProjects | undefined;
};

export type CreateConnectorResponseBody = {
  /**
   * Stable `scl_` connector ID.
   */
  id: string;
  /**
   * Vercel team ID that owns the connector.
   */
  ownerId: string;
  /**
   * Creation time in epoch milliseconds.
   */
  createdAt: number;
  /**
   * Last update time in epoch milliseconds.
   */
  updatedAt: number;
  /**
   * Deletion time in epoch milliseconds, when the connector is deleted.
   */
  deletedAt?: number | undefined;
  /**
   * Time when this connector started requiring reinstallation because an installation-affecting app-token grant changed.
   */
  reinstallAt?: number | undefined;
  createdBy?: CreatedBy1 | CreatedBy2 | undefined;
  updatedBy?: CreateConnectorUpdatedBy1 | CreateConnectorUpdatedBy2 | undefined;
  /**
   * How the connector row was originally created. New create paths stamp this explicitly; older rows may omit it.
   */
  creationMode?: CreateConnectorCreationMode | undefined;
  /**
   * Managed connector metadata exposed without leaking the manager connector or installation identifiers.
   */
  managed?: CreateConnectorManaged | undefined;
  /**
   * Whether the connector is available outside its owning team.
   */
  public: boolean;
  /**
   * Team-scoped connector UID.
   */
  uid: string;
  /**
   * Connector implementation type.
   */
  type: CreateConnectorType;
  /**
   * Best-effort identifier of the third-party service this connector represents, independent of `type`. Examples: `'slack'`, `'mcp.linear.app'`, and `'auth.example.com'`. Always present in API responses.
   */
  service: string;
  /**
   * The connection method this connector was created from, when the create request named one.
   */
  connectionMethod?: string | undefined;
  /**
   * Which of the service's products/surfaces this connector points at.
   */
  target?: string | undefined;
  /**
   * Connector name within the owning team.
   */
  name: string;
  /**
   * Human-readable connector name.
   */
  displayName: string;
  /**
   * Provider-side URL for viewing or managing the resource represented by the connector. The destination can be an app, account, phone line, or service instance, depending on the connector type.
   */
  clientUrl?: string | null | undefined;
  /**
   * Redirect URI registered with the third-party service for this connector, if any. Used by `startAuthorization`/`startInstallation` to replay the exact URI back to the provider's token endpoint. Absent on connectors created before this field was introduced; those callers fall back to the `https://connect.vercel.com/callback` default.
   */
  redirectUri?: string | undefined;
  /**
   * Installation used when a token request omits an installation ID.
   */
  defaultInstallationId?: string | undefined;
  /**
   * Provider configuration stored for this connector.
   */
  data: { [k: string]: any };
  /**
   * Human-readable name of the connector type.
   */
  typeName: string;
  /**
   * Icon identifier supplied by the connector type.
   */
  typeIcon?: string | undefined;
  /**
   * Public website for the connected service.
   */
  website?: string | undefined;
  /**
   * Developer website for the connected service.
   */
  devsite?: string | undefined;
  /**
   * Developer documentation for the connected service.
   */
  docsite?: string | undefined;
  /**
   * Connector branding icon. SHA-1 hash that resolves to the uploaded icon through the Vercel avatar service. Consumers render this with `https://vercel.com/api/www/avatar/{icon}`.
   */
  icon?: string | undefined;
  /**
   * Hex background color (e.g., `#000000`) for branding.
   */
  backgroundColor?: string | undefined;
  /**
   * Hex accent color (e.g., `#000000`) for branding.
   */
  accentColor?: string | undefined;
  /**
   * Token subject types supported by the connector.
   */
  supportedSubjectTypes: Array<string>;
  appTokens?: CreateConnectorAppTokens | undefined;
  userTokens?: CreateConnectorUserTokens | undefined;
  /**
   * Whether the connector supports an installation flow.
   */
  supportsInstallation: boolean;
  /**
   * Whether Connect can revoke tokens for this connector.
   */
  supportsRevocation: boolean;
  /**
   * Provider tenant ID associated with the connector owner.
   */
  ownerTenantId?: string | undefined;
  /**
   * Whether this connector type supports trigger webhooks. Derived from the type definition; indicates that `triggers` and `triggerDestinations` may be meaningful for this connector.
   */
  supportsTriggers: boolean;
  supportsIcon: CreateConnectorSupportsIcon;
  /**
   * Whether this connector can verify and list messaging contacts. Derived from the type definition's `supportsContacts`; indicates that the `/connect/connectors/:connectorId/contacts` endpoints are meaningful for this connector.
   */
  supportsContacts?: boolean | undefined;
  /**
   * Incoming trigger configuration. Only present when enabled.
   */
  triggers?: ConnectTriggerConfiguration | undefined;
  /**
   * Known events this connector subscribes to (e.g. Slack bot events, GitHub webhook events). Names are type-specific and validated by the managed-create flow when forwarded to the third-party service.
   */
  events?: Array<string> | undefined;
  /**
   * Destinations that incoming triggers should be forwarded to. Limited to 3 entries. Set the initial destination with `triggerDestination` during creation. Replace the complete set with `PATCH /v1/connect/connectors/{connector}/trigger-destinations`.
   */
  triggerDestinations?: Array<ConnectTriggerDestination> | undefined;
  /**
   * Whether this connector is linked to the project supplied through `prioritizedProjectId`. Only present on prioritized list responses.
   */
  isConnectedToPrioritizedProject?: boolean | undefined;
  /**
   * Optional expansions populated by `?include=...` on the list endpoint.
   */
  includes?: Includes | undefined;
};

export type CreateConnectorResponse =
  | CreateConnectorResponseBody
  | ConnectConnectorCreateResult;

/** @internal */
export type CreateConnectorRequest$Outbound = {
  teamId?: string | undefined;
  slug?: string | undefined;
  ConnectCreateConnectorRequest?:
    | ConnectCreateConnectorRequest$Outbound
    | undefined;
};

/** @internal */
export const CreateConnectorRequest$outboundSchema: z.ZodType<
  CreateConnectorRequest$Outbound,
  z.ZodTypeDef,
  CreateConnectorRequest
> = z.object({
  teamId: z.string().optional(),
  slug: z.string().optional(),
  connectCreateConnectorRequest: ConnectCreateConnectorRequest$outboundSchema
    .optional(),
}).transform((v) => {
  return remap$(v, {
    connectCreateConnectorRequest: "ConnectCreateConnectorRequest",
  });
});

export function createConnectorRequestToJSON(
  createConnectorRequest: CreateConnectorRequest,
): string {
  return JSON.stringify(
    CreateConnectorRequest$outboundSchema.parse(createConnectorRequest),
  );
}

/** @internal */
export const Environment2$inboundSchema: z.ZodNativeEnum<typeof Environment2> =
  z.nativeEnum(Environment2);

/** @internal */
export const CreateConnectorCreatedByEnvironment$inboundSchema: z.ZodType<
  CreateConnectorCreatedByEnvironment,
  z.ZodTypeDef,
  unknown
> = smartUnion([types.string(), Environment2$inboundSchema]);

export function createConnectorCreatedByEnvironmentFromJSON(
  jsonString: string,
): SafeParseResult<CreateConnectorCreatedByEnvironment, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      CreateConnectorCreatedByEnvironment$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateConnectorCreatedByEnvironment' from JSON`,
  );
}

/** @internal */
export const CreatedBy2$inboundSchema: z.ZodType<
  CreatedBy2,
  z.ZodTypeDef,
  unknown
> = z.object({
  type: types.literal("project"),
  id: types.string(),
  environment: smartUnion([types.string(), Environment2$inboundSchema]),
});

export function createdBy2FromJSON(
  jsonString: string,
): SafeParseResult<CreatedBy2, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreatedBy2$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreatedBy2' from JSON`,
  );
}

/** @internal */
export const CreatedBy1$inboundSchema: z.ZodType<
  CreatedBy1,
  z.ZodTypeDef,
  unknown
> = z.object({
  type: types.literal("user"),
  id: types.string(),
});

export function createdBy1FromJSON(
  jsonString: string,
): SafeParseResult<CreatedBy1, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreatedBy1$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreatedBy1' from JSON`,
  );
}

/** @internal */
export const CreateConnectorCreatedBy$inboundSchema: z.ZodType<
  CreateConnectorCreatedBy,
  z.ZodTypeDef,
  unknown
> = z.union([
  z.lazy(() => CreatedBy1$inboundSchema),
  z.lazy(() => CreatedBy2$inboundSchema),
]);

export function createConnectorCreatedByFromJSON(
  jsonString: string,
): SafeParseResult<CreateConnectorCreatedBy, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateConnectorCreatedBy$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateConnectorCreatedBy' from JSON`,
  );
}

/** @internal */
export const CreateConnectorEnvironment2$inboundSchema: z.ZodNativeEnum<
  typeof CreateConnectorEnvironment2
> = z.nativeEnum(CreateConnectorEnvironment2);

/** @internal */
export const CreateConnectorUpdatedByEnvironment$inboundSchema: z.ZodType<
  CreateConnectorUpdatedByEnvironment,
  z.ZodTypeDef,
  unknown
> = smartUnion([types.string(), CreateConnectorEnvironment2$inboundSchema]);

export function createConnectorUpdatedByEnvironmentFromJSON(
  jsonString: string,
): SafeParseResult<CreateConnectorUpdatedByEnvironment, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      CreateConnectorUpdatedByEnvironment$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateConnectorUpdatedByEnvironment' from JSON`,
  );
}

/** @internal */
export const CreateConnectorUpdatedBy2$inboundSchema: z.ZodType<
  CreateConnectorUpdatedBy2,
  z.ZodTypeDef,
  unknown
> = z.object({
  type: types.literal("project"),
  id: types.string(),
  environment: smartUnion([
    types.string(),
    CreateConnectorEnvironment2$inboundSchema,
  ]),
});

export function createConnectorUpdatedBy2FromJSON(
  jsonString: string,
): SafeParseResult<CreateConnectorUpdatedBy2, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateConnectorUpdatedBy2$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateConnectorUpdatedBy2' from JSON`,
  );
}

/** @internal */
export const CreateConnectorUpdatedBy1$inboundSchema: z.ZodType<
  CreateConnectorUpdatedBy1,
  z.ZodTypeDef,
  unknown
> = z.object({
  type: types.literal("user"),
  id: types.string(),
});

export function createConnectorUpdatedBy1FromJSON(
  jsonString: string,
): SafeParseResult<CreateConnectorUpdatedBy1, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateConnectorUpdatedBy1$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateConnectorUpdatedBy1' from JSON`,
  );
}

/** @internal */
export const CreateConnectorUpdatedBy$inboundSchema: z.ZodType<
  CreateConnectorUpdatedBy,
  z.ZodTypeDef,
  unknown
> = z.union([
  z.lazy(() => CreateConnectorUpdatedBy1$inboundSchema),
  z.lazy(() => CreateConnectorUpdatedBy2$inboundSchema),
]);

export function createConnectorUpdatedByFromJSON(
  jsonString: string,
): SafeParseResult<CreateConnectorUpdatedBy, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateConnectorUpdatedBy$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateConnectorUpdatedBy' from JSON`,
  );
}

/** @internal */
export const CreateConnectorCreationMode$inboundSchema: z.ZodNativeEnum<
  typeof CreateConnectorCreationMode
> = z.nativeEnum(CreateConnectorCreationMode);

/** @internal */
export const CreateConnectorManaged$inboundSchema: z.ZodType<
  CreateConnectorManaged,
  z.ZodTypeDef,
  unknown
> = z.object({
  sync: types.optional(types.boolean()),
});

export function createConnectorManagedFromJSON(
  jsonString: string,
): SafeParseResult<CreateConnectorManaged, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateConnectorManaged$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateConnectorManaged' from JSON`,
  );
}

/** @internal */
export const CreateConnectorType$inboundSchema: z.ZodNativeEnum<
  typeof CreateConnectorType
> = z.nativeEnum(CreateConnectorType);

/** @internal */
export const CreateConnectorAppTokens$inboundSchema: z.ZodType<
  CreateConnectorAppTokens,
  z.ZodTypeDef,
  unknown
> = z.object({
  crossInstallation: types.boolean(),
  supportsRefinement: types.boolean(),
  requiresReinstallation: types.optional(types.boolean()),
  scopes: types.optional(z.array(types.string())),
  supportedAuthorizationDetails: types.optional(z.array(types.string())),
  permissionsUrl: types.optional(types.string()),
});

export function createConnectorAppTokensFromJSON(
  jsonString: string,
): SafeParseResult<CreateConnectorAppTokens, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateConnectorAppTokens$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateConnectorAppTokens' from JSON`,
  );
}

/** @internal */
export const CreateConnectorUserTokens$inboundSchema: z.ZodType<
  CreateConnectorUserTokens,
  z.ZodTypeDef,
  unknown
> = z.object({
  crossInstallation: types.boolean(),
  supportsRefinement: types.boolean(),
  scopes: types.optional(z.array(types.string())),
  supportedAuthorizationDetails: types.optional(z.array(types.string())),
  manualCredentialInput: types.optional(types.boolean()),
});

export function createConnectorUserTokensFromJSON(
  jsonString: string,
): SafeParseResult<CreateConnectorUserTokens, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateConnectorUserTokens$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateConnectorUserTokens' from JSON`,
  );
}

/** @internal */
export const CreateConnectorSupportsIcon$inboundSchema: z.ZodNativeEnum<
  typeof CreateConnectorSupportsIcon
> = z.nativeEnum(CreateConnectorSupportsIcon);

/** @internal */
export const CreateConnectorCustomEnvironments$inboundSchema: z.ZodType<
  CreateConnectorCustomEnvironments,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: types.string(),
  slug: types.string(),
});

export function createConnectorCustomEnvironmentsFromJSON(
  jsonString: string,
): SafeParseResult<CreateConnectorCustomEnvironments, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateConnectorCustomEnvironments$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateConnectorCustomEnvironments' from JSON`,
  );
}

/** @internal */
export const CreateConnectorProject$inboundSchema: z.ZodType<
  CreateConnectorProject,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: types.string(),
  name: types.string(),
  customEnvironments: types.optional(
    z.array(z.lazy(() => CreateConnectorCustomEnvironments$inboundSchema)),
  ),
});

export function createConnectorProjectFromJSON(
  jsonString: string,
): SafeParseResult<CreateConnectorProject, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateConnectorProject$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateConnectorProject' from JSON`,
  );
}

/** @internal */
export const CreateConnectorEnvironments2$inboundSchema: z.ZodNativeEnum<
  typeof CreateConnectorEnvironments2
> = z.nativeEnum(CreateConnectorEnvironments2);

/** @internal */
export const CreateConnectorEnvironments$inboundSchema: z.ZodType<
  CreateConnectorEnvironments,
  z.ZodTypeDef,
  unknown
> = smartUnion([types.string(), CreateConnectorEnvironments2$inboundSchema]);

export function createConnectorEnvironmentsFromJSON(
  jsonString: string,
): SafeParseResult<CreateConnectorEnvironments, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateConnectorEnvironments$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateConnectorEnvironments' from JSON`,
  );
}

/** @internal */
export const CreateConnectorItems$inboundSchema: z.ZodType<
  CreateConnectorItems,
  z.ZodTypeDef,
  unknown
> = z.object({
  clientId: types.string(),
  project: types.optional(z.lazy(() => CreateConnectorProject$inboundSchema)),
  environments: z.array(
    smartUnion([types.string(), CreateConnectorEnvironments2$inboundSchema]),
  ),
  createdAt: types.number(),
  updatedAt: types.number(),
});

export function createConnectorItemsFromJSON(
  jsonString: string,
): SafeParseResult<CreateConnectorItems, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateConnectorItems$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateConnectorItems' from JSON`,
  );
}

/** @internal */
export const CreateConnectorPagination$inboundSchema: z.ZodType<
  CreateConnectorPagination,
  z.ZodTypeDef,
  unknown
> = z.object({
  next: types.nullable(types.string()),
});

export function createConnectorPaginationFromJSON(
  jsonString: string,
): SafeParseResult<CreateConnectorPagination, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateConnectorPagination$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateConnectorPagination' from JSON`,
  );
}

/** @internal */
export const CreateConnectorProjects$inboundSchema: z.ZodType<
  CreateConnectorProjects,
  z.ZodTypeDef,
  unknown
> = z.object({
  items: z.array(z.lazy(() => CreateConnectorItems$inboundSchema)),
  hasMore: types.boolean(),
  pagination: z.lazy(() => CreateConnectorPagination$inboundSchema),
});

export function createConnectorProjectsFromJSON(
  jsonString: string,
): SafeParseResult<CreateConnectorProjects, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateConnectorProjects$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateConnectorProjects' from JSON`,
  );
}

/** @internal */
export const Includes$inboundSchema: z.ZodType<
  Includes,
  z.ZodTypeDef,
  unknown
> = z.object({
  projects: types.optional(z.lazy(() => CreateConnectorProjects$inboundSchema)),
});

export function includesFromJSON(
  jsonString: string,
): SafeParseResult<Includes, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Includes$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Includes' from JSON`,
  );
}

/** @internal */
export const CreateConnectorResponseBody$inboundSchema: z.ZodType<
  CreateConnectorResponseBody,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: types.string(),
  ownerId: types.string(),
  createdAt: types.number(),
  updatedAt: types.number(),
  deletedAt: types.optional(types.number()),
  reinstallAt: types.optional(types.number()),
  createdBy: types.optional(
    z.union([
      z.lazy(() => CreatedBy1$inboundSchema),
      z.lazy(() => CreatedBy2$inboundSchema),
    ]),
  ),
  updatedBy: types.optional(
    z.union([
      z.lazy(() => CreateConnectorUpdatedBy1$inboundSchema),
      z.lazy(() => CreateConnectorUpdatedBy2$inboundSchema),
    ]),
  ),
  creationMode: types.optional(CreateConnectorCreationMode$inboundSchema),
  managed: types.optional(z.lazy(() => CreateConnectorManaged$inboundSchema)),
  public: types.boolean(),
  uid: types.string(),
  type: CreateConnectorType$inboundSchema,
  service: types.string(),
  connectionMethod: types.optional(types.string()),
  target: types.optional(types.string()),
  name: types.string(),
  displayName: types.string(),
  clientUrl: z.nullable(types.string()).optional(),
  redirectUri: types.optional(types.string()),
  defaultInstallationId: types.optional(types.string()),
  data: z.record(z.any()),
  typeName: types.string(),
  typeIcon: types.optional(types.string()),
  website: types.optional(types.string()),
  devsite: types.optional(types.string()),
  docsite: types.optional(types.string()),
  icon: types.optional(types.string()),
  backgroundColor: types.optional(types.string()),
  accentColor: types.optional(types.string()),
  supportedSubjectTypes: z.array(types.string()),
  appTokens: types.optional(
    z.lazy(() => CreateConnectorAppTokens$inboundSchema),
  ),
  userTokens: types.optional(
    z.lazy(() => CreateConnectorUserTokens$inboundSchema),
  ),
  supportsInstallation: types.boolean(),
  supportsRevocation: types.boolean(),
  ownerTenantId: types.optional(types.string()),
  supportsTriggers: types.boolean(),
  supportsIcon: CreateConnectorSupportsIcon$inboundSchema,
  supportsContacts: types.optional(types.boolean()),
  triggers: types.optional(ConnectTriggerConfiguration$inboundSchema),
  events: types.optional(z.array(types.string())),
  triggerDestinations: types.optional(
    z.array(ConnectTriggerDestination$inboundSchema),
  ),
  isConnectedToPrioritizedProject: types.optional(types.boolean()),
  includes: types.optional(z.lazy(() => Includes$inboundSchema)),
});

export function createConnectorResponseBodyFromJSON(
  jsonString: string,
): SafeParseResult<CreateConnectorResponseBody, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateConnectorResponseBody$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateConnectorResponseBody' from JSON`,
  );
}

/** @internal */
export const CreateConnectorResponse$inboundSchema: z.ZodType<
  CreateConnectorResponse,
  z.ZodTypeDef,
  unknown
> = smartUnion([
  z.lazy(() => CreateConnectorResponseBody$inboundSchema),
  ConnectConnectorCreateResult$inboundSchema,
]);

export function createConnectorResponseFromJSON(
  jsonString: string,
): SafeParseResult<CreateConnectorResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateConnectorResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateConnectorResponse' from JSON`,
  );
}
