/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { smartUnion } from "../types/smartUnion.js";
import { SDKValidationError } from "./sdkvalidationerror.js";

export type SearchDomainsRequestBody = {
  /**
   * an array of at most 200 item(s)
   */
  domains: Array<string>;
};

export type SearchDomainsRequest = {
  teamId?: string | undefined;
  requestBody: SearchDomainsRequestBody;
};

export type Results2 = {
  /**
   * A valid domain name
   */
  domain: string;
  available: true;
  years: number;
  price: number;
  renewalPrice: number;
  premium: boolean;
};

export type Results1 = {
  /**
   * A valid domain name
   */
  domain: string;
  /**
   * `false` means the domain is unavailable or its availability could not be confirmed.
   */
  available: false;
};

export type Results = Results2 | Results1;

/**
 * Success
 */
export type SearchDomainsResponseBody = {
  results: Array<Results2 | Results1>;
};

/** @internal */
export type SearchDomainsRequestBody$Outbound = {
  domains: Array<string>;
};

/** @internal */
export const SearchDomainsRequestBody$outboundSchema: z.ZodType<
  SearchDomainsRequestBody$Outbound,
  z.ZodTypeDef,
  SearchDomainsRequestBody
> = z.object({
  domains: z.array(z.string()),
});

export function searchDomainsRequestBodyToJSON(
  searchDomainsRequestBody: SearchDomainsRequestBody,
): string {
  return JSON.stringify(
    SearchDomainsRequestBody$outboundSchema.parse(searchDomainsRequestBody),
  );
}

/** @internal */
export type SearchDomainsRequest$Outbound = {
  teamId?: string | undefined;
  RequestBody: SearchDomainsRequestBody$Outbound;
};

/** @internal */
export const SearchDomainsRequest$outboundSchema: z.ZodType<
  SearchDomainsRequest$Outbound,
  z.ZodTypeDef,
  SearchDomainsRequest
> = z.object({
  teamId: z.string().optional(),
  requestBody: z.lazy(() => SearchDomainsRequestBody$outboundSchema),
}).transform((v) => {
  return remap$(v, {
    requestBody: "RequestBody",
  });
});

export function searchDomainsRequestToJSON(
  searchDomainsRequest: SearchDomainsRequest,
): string {
  return JSON.stringify(
    SearchDomainsRequest$outboundSchema.parse(searchDomainsRequest),
  );
}

/** @internal */
export const Results2$inboundSchema: z.ZodType<
  Results2,
  z.ZodTypeDef,
  unknown
> = z.object({
  domain: types.string(),
  available: types.literal(true),
  years: types.number(),
  price: types.number(),
  renewalPrice: types.number(),
  premium: types.boolean(),
});

export function results2FromJSON(
  jsonString: string,
): SafeParseResult<Results2, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Results2$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Results2' from JSON`,
  );
}

/** @internal */
export const Results1$inboundSchema: z.ZodType<
  Results1,
  z.ZodTypeDef,
  unknown
> = z.object({
  domain: types.string(),
  available: types.literal(false),
});

export function results1FromJSON(
  jsonString: string,
): SafeParseResult<Results1, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Results1$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Results1' from JSON`,
  );
}

/** @internal */
export const Results$inboundSchema: z.ZodType<Results, z.ZodTypeDef, unknown> =
  smartUnion([
    z.lazy(() => Results2$inboundSchema),
    z.lazy(() => Results1$inboundSchema),
  ]);

export function resultsFromJSON(
  jsonString: string,
): SafeParseResult<Results, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Results$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Results' from JSON`,
  );
}

/** @internal */
export const SearchDomainsResponseBody$inboundSchema: z.ZodType<
  SearchDomainsResponseBody,
  z.ZodTypeDef,
  unknown
> = z.object({
  results: z.array(
    smartUnion([
      z.lazy(() => Results2$inboundSchema),
      z.lazy(() => Results1$inboundSchema),
    ]),
  ),
});

export function searchDomainsResponseBodyFromJSON(
  jsonString: string,
): SafeParseResult<SearchDomainsResponseBody, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchDomainsResponseBody$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchDomainsResponseBody' from JSON`,
  );
}
